# CaMa-Flood: Mac installation evidence

The real Mac DS attempt built MAIN_cmf and the independent receipt confirmed native Mach-O shape and all declared imports. Its retained startup output reached cmf_ctrl_nmlist_mod.F90 and reported an absent input_cmf.nam. That is project input, not an absent interpreter: the verdict was audited to installed after a narrow classifier fix, with the original evidence preserved. No scientific run was performed.

Keep the model at the KI's canonical binaries/cmf_v420_pkg/src/MAIN_cmf path, or provide a manifest that resolves the actual native product correctly. Prefer cloning/building into the canonical prefix over creating divergent source copies. The observed native build used gfortran, netCDF-Fortran/netCDF-C under /opt/homebrew, and OpenMP; retain the upstream precision and preprocessor configuration when adapting adm/Mkinclude to the host SDK.

Probe from an empty directory. Missing input_cmf.nam is expected without a project, while missing shared libraries or helper programs remain installation failures. Do not fetch river-network maps, runoff data or a reference namelist just to make a startup probe run a simulation. Record the actual git commit rather than inferring provenance from package-directory or embedded version names.
