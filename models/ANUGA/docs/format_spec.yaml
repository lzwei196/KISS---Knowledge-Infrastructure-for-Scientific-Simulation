model:
  name: ANUGA Skill
  domain: flood
  binary_path: ''
  language: unknown
inputs:
  forcing: []
outputs:
  primary:
  - name: discharge
    unit: '`anuga.get_flow_through_cross_section()` on SWW'
    format: ''
    source: skill_md
  - name: flood_depth
    canonical_id: flood_depth
    unit: m
  - name: water_surface_elevation
    canonical_id: water_surface_elevation
    unit: m
  - name: inundation_extent
    canonical_id: inundation_extent
    unit: km2
known_issues:
- id: dt_anuga_001
  description: '{''description'': ''Entire domain floods unrealistically; flood depths
    are 1000x too high even for moderate rainfall events. Rate_operator appears to
    inject vastly too much water.\n''}'
  affected_output: '{''root_cause'': ''Rainfall rate passed to Rate_operator in mm/hr
    instead of m/s. ANUGA Rate_operator expects rainfall in m/s. If CMFD precipitation
    (kg/m2/s, numerically equivalent to mm/s) or mm/hr val'
  detection: '{''root_cause'': ''Rainfall rate passed to Rate_operator in mm/hr instead
    of m/s. ANUGA Rate_operator expects rainfall in m/s. If CMFD precipitation (kg/m2/s,
    numerically equivalent to mm/s) or mm/hr val'
  fix: '{''action'': ''Convert rainfall to m/s before passing to Rate_operator. CMFD:
    value is kg/m2/s = mm/s, so divide by 1000 to get m/s. MSWX: mm/3hr, divide by
    (1000 * 10800). NASA POWER: mm/hr, divide by ('
- id: dt_anuga_002
  description: '{''description'': ''No flooding at all despite heavy rainfall event
    being applied. Rate_operator is active but contributes negligible water volume.
    Max depth remains near zero throughout the simulation.\n''}'
  affected_output: '{''root_cause'': ''Rainfall rate double-divided: already in m/s
    but divided by 1000 again (treating m/s as mm/s). For example, CMFD load_hourly_forcing
    returns precip_mm (mm per timestep). The convert_fo'
  detection: '{''root_cause'': ''Rainfall rate double-divided: already in m/s but
    divided by 1000 again (treating m/s as mm/s). For example, CMFD load_hourly_forcing
    returns precip_mm (mm per timestep). The convert_fo'
  fix: '{''action'': ''Trace the unit chain: CMFD raw = kg/m2/s. load_hourly_forcing
    returns mm/timestep (already multiplied by timestep_s). To get m/s: divide by
    1000 (mm to m) then by timestep_s (per-timestep '
- id: dt_anuga_003
  description: '{''description'': ''DEM elevations appear shifted by 20-40m compared
    to expected terrain. Simulated water surface elevation (stage) is systematically
    offset from reference gauge data. Flood extent is wrong because terrain is too
    high or too low relative to boundary water levels.\n''}'
  affected_output: '{''root_cause'': ''Vertical datum mismatch between DEM and boundary
    condition water levels. China DEM 90m uses EGM96 geoid heights. If boundary water
    levels are referenced to WGS84 ellipsoid or local cha'
  detection: '{''root_cause'': ''Vertical datum mismatch between DEM and boundary
    condition water levels. China DEM 90m uses EGM96 geoid heights. If boundary water
    levels are referenced to WGS84 ellipsoid or local cha'
  fix: '{''action'': ''Ensure DEM and all water level boundary conditions use the
    same vertical datum. If DEM is EGM96-geoid and boundary levels are ellipsoidal,
    subtract the local geoid undulation from the DEM '
- id: dt_anuga_004
  description: '{''description'': "run_anuga.py crashes with KeyError or ValueError
    when reading the forcing CSV. Error message references missing column name ''time_seconds''
    or ''rainfall_m_per_s''.\n"}'
  affected_output: '{''root_cause'': "The rainfall CSV file does not have the expected
    header row (time_seconds, rainfall_m_per_s). The file may have been hand-edited,
    saved from Excel with different delimiters, or generat'
  detection: '{''root_cause'': "The rainfall CSV file does not have the expected header
    row (time_seconds, rainfall_m_per_s). The file may have been hand-edited, saved
    from Excel with different delimiters, or generat'
  fix: '{''action'': ''Ensure the forcing CSV has exactly two columns with header:
    time_seconds,rainfall_m_per_s. Use convert_forcing_to_anuga.py to regenerate the
    file. If editing manually, verify no BOM charac'
- id: dt_anuga_005
  description: '{''description'': "parse_anuga_output.py fails with ''NetCDFFile''
    or ''No such variable'' error when reading the SWW file. The SWW file exists and
    has nonzero size but cannot be parsed.\n"}'
  affected_output: '{''root_cause'': ''The SWW file was produced by a parallel ANUGA
    run and consists of multiple per-processor SWW fragments (e.g., anuga_sim_P0_0.sww,
    anuga_sim_P1_0.sww) that were not merged. The expected'
  detection: '{''root_cause'': ''The SWW file was produced by a parallel ANUGA run
    and consists of multiple per-processor SWW fragments (e.g., anuga_sim_P0_0.sww,
    anuga_sim_P1_0.sww) that were not merged. The expected'
  fix: '{''action'': ''After domain.evolve completes, call domain.sww_merge(delete_old=True)
    to combine parallel fragments. If merge fails, run manually: python -c "import
    anuga; anuga.sww_merge(\''anuga_sim\'', d'
- id: dt_anuga_006
  description: '{''description'': ''SWW output file is missing or zero bytes after
    simulation completes without error. run_summary.json shows sww_exists=false. The
    ANUGA log shows evolve loop completed normally.\n''}'
  affected_output: '{''root_cause'': ''domain.set_datadir() was called with a path
    that does not exist, or domain.set_name() was not called so the output goes to
    an unexpected default filename. ANUGA writes SWW to datadir/n'
  detection: '{''root_cause'': ''domain.set_datadir() was called with a path that
    does not exist, or domain.set_name() was not called so the output goes to an unexpected
    default filename. ANUGA writes SWW to datadir/n'
  fix: '{''action'': ''Ensure os.makedirs(output_dir, exist_ok=True) is called before
    domain creation. Verify domain.set_datadir(output_dir) and domain.set_name("anuga_sim")
    are both called. After simulation, ch'
- id: dt_anuga_007
  description: '{''description'': ''ANUGA produces different results between runs
    with identical inputs. Or: stage profile has unexpected oscillations near boundaries
    that differ between flow algorithms. Analytical benchmark (dam_break_wet) fails
    NSE threshold.\n''}'
  affected_output: '{''root_cause'': "Incorrect or missing flow algorithm setting.
    ANUGA supports multiple finite-volume schemes: ''DE0'' (default recommended, discontinuous
    elevation), ''DE1'' (higher order), ''1_5'' (original '
  detection: '{''root_cause'': "Incorrect or missing flow algorithm setting. ANUGA
    supports multiple finite-volume schemes: ''DE0'' (default recommended, discontinuous
    elevation), ''DE1'' (higher order), ''1_5'' (original '
  fix: '{''action'': "Explicitly call domain.set_flow_algorithm(''DE0'') after domain
    creation. For benchmarks requiring higher accuracy, try ''DE1''. Avoid leaving
    the flow algorithm unset. The run_anuga.py tool a'
- id: dt_anuga_008
  description: '{''description'': ''Water accumulates at domain edges and does not
    leave. The simulation shows artificially high water levels at boundaries. Total
    water volume in the domain increases over time even after rainfall stops.\n''}'
  affected_output: '{''root_cause'': ''All boundaries set to Reflective_boundary.
    Reflective boundaries act as solid walls, preventing water from exiting the domain.
    For catchment simulations, the downstream outlet boundary'
  detection: '{''root_cause'': ''All boundaries set to Reflective_boundary. Reflective
    boundaries act as solid walls, preventing water from exiting the domain. For catchment
    simulations, the downstream outlet boundary'
  fix: '{''action'': "Set the outlet/downstream boundary to anuga.Transmissive_boundary(domain)
    to allow free outflow. Keep Reflective_boundary on upslope/lateral boundaries.
    In run_anuga.py, the ''bottom'' bound'
- id: dt_anuga_009
  description: '{''description'': "ANUGA raises an error during domain creation: ''create_domain_from_regions:
    boundary_tags do not match polygon edges''. Or the mesh is created but boundary
    conditions cannot be applied because tag names do not match.\n"}'
  affected_output: '{''root_cause'': ''The boundary_tags dictionary in create_domain_from_regions
    must map tag names to polygon edge indices (0-based). For a rectangular bounding
    polygon with 4 vertices [SW, SE, NE, NW], th'
  detection: '{''root_cause'': ''The boundary_tags dictionary in create_domain_from_regions
    must map tag names to polygon edge indices (0-based). For a rectangular bounding
    polygon with 4 vertices [SW, SE, NE, NW], th'
  fix: '{''action'': ''Verify that boundary_tags covers all polygon edges. For a 4-vertex
    rectangle: {"bottom": [0], "right": [1], "top": [2], "left": [3]}. The tag names
    in set_boundary() must exactly match the'
- id: dt_anuga_010
  description: '{''description'': ''Flood wave propagates much faster than expected.
    Water reaches the domain outlet in a fraction of the expected travel time. Peak
    discharge is too high and too early compared to observations.\n''}'
  affected_output: '{''root_cause'': "Manning''s n set too low (e.g., 0.001 or 0.0).
    Low friction allows unrealistically fast flow velocities. For real-world catchments,
    Manning''s n should be 0.02-0.15 depending on land cov'
  detection: '{''root_cause'': "Manning''s n set too low (e.g., 0.001 or 0.0). Low
    friction allows unrealistically fast flow velocities. For real-world catchments,
    Manning''s n should be 0.02-0.15 depending on land cov'
  fix: '{''action'': "Set domain.set_quantity(''friction'', n) with appropriate Manning''s
    n: 0.02-0.03 for smooth channels, 0.03-0.05 for natural streams, 0.05-0.10 for
    floodplains with vegetation, 0.10-0.15 for '
- id: dt_anuga_011
  description: '{''description'': ''Simulation takes hours or days for a modest domain.
    Memory usage is extremely high (>16 GB). The domain has millions of triangles
    for a 5-10 km extent.\n''}'
  affected_output: '{''root_cause'': ''maximum_triangle_area set too small relative
    to domain size. ANUGA cost scales as O(N * T/dt) where N = number of triangles.
    For a 5000m x 5000m domain, max_area=1 m2 produces ~25 mill'
  detection: '{''root_cause'': ''maximum_triangle_area set too small relative to domain
    size. ANUGA cost scales as O(N * T/dt) where N = number of triangles. For a 5000m
    x 5000m domain, max_area=1 m2 produces ~25 mill'
  fix: '{''action'': ''For real-world flood modeling, use max_area = 100-1000 m2 for
    domains up to 10 km. For analytical benchmarks, use max_area = 1-10 m2 for accuracy.
    Check mesh size with len(domain) after cr'
- id: dt_anuga_012
  description: '{''description'': "Simulation starts with an initial ''splash'' or
    shock wave even though initial conditions should be calm (dry bed or flat water).
    The first few timesteps show large transient oscillations that corrupt the early-time
    solution.\n"}'
  affected_output: '{''root_cause'': ''Initial stage set to a constant value (e.g.,
    0.0) rather than to the bed elevation. When stage < elevation, ANUGA clips depth
    to zero, but when stage > elevation in some cells and < el'
  detection: '{''root_cause'': ''Initial stage set to a constant value (e.g., 0.0)
    rather than to the bed elevation. When stage < elevation, ANUGA clips depth to
    zero, but when stage > elevation in some cells and < el'
  fix: '{''action'': "For dry-bed simulations, set: domain.set_quantity(''stage'',
    expression=''elevation''). This ensures depth = stage - elevation = 0 everywhere.
    For partially wet domains, ensure stage >= elevat'
- id: dt_anuga_013
  description: '{''description'': "Simulation crashes with NaN or Inf values in stage
    or momentum after running for some time. The log may show ''WARNING: set_timestep
    is zero'' or the evolve loop raises a FloatingPointError.\n"}'
  affected_output: '{''root_cause'': ''CFL violation causing numerical instability.
    ANUGA uses adaptive timestepping with CFL condition: dt <= CFL * min(dx / sqrt(g*h))
    across all triangles. If a single triangle has very sm'
  detection: '{''root_cause'': ''CFL violation causing numerical instability. ANUGA
    uses adaptive timestepping with CFL condition: dt <= CFL * min(dx / sqrt(g*h))
    across all triangles. If a single triangle has very sm'
  fix: '{''action'': "Check mesh quality: avoid degenerate triangles by ensuring the
    mesh generator has reasonable minimum angle constraints. ANUGA''s default mesh
    generator (Triangle) usually handles this, but '
- id: dt_anuga_014
  description: '{''description'': ''Checkerboard pattern in water depth: alternating
    wet and dry cells across the mesh. Small spurious oscillations appear near the
    wetting front. The wet/dry interface shows unrealistic behavior.\n''}'
  affected_output: '{''root_cause'': ''Wet/dry instability in the shallow water solver.
    ANUGA handles wetting and drying, but on coarse meshes or with abrupt topography
    transitions, the wetting front can oscillate. The DE0 '
  detection: '{''root_cause'': ''Wet/dry instability in the shallow water solver.
    ANUGA handles wetting and drying, but on coarse meshes or with abrupt topography
    transitions, the wetting front can oscillate. The DE0 '
  fix: '{''action'': "Increase mesh resolution near the expected wetting front using
    interior_regions with smaller max_area. Ensure the ''DE0'' flow algorithm is selected
    (most stable for wet/dry). Adding a small'
- id: dt_anuga_015
  description: '{''description'': ''Total water volume in the domain does not balance:
    rainfall input minus outflow does not equal stored volume change. Mass conservation
    error exceeds 5-10% over the simulation.\n''}'
  affected_output: '{''root_cause'': ''Transmissive boundaries can both remove and
    inject water depending on local flow conditions. If the boundary water depth oscillates,
    small amounts of water can be spuriously injected a'
  detection: '{''root_cause'': ''Transmissive boundaries can both remove and inject
    water depending on local flow conditions. If the boundary water depth oscillates,
    small amounts of water can be spuriously injected a'
  fix: '{''action'': ''Check mass balance with domain.report_water_volume_statistics().
    Reduce yieldstep to capture transient behavior. For strict mass conservation,
    use Dirichlet_boundary with a prescribed cons'
- id: dt_anuga_016
  description: '{''description'': "''import anuga'' fails with ImportError referencing
    a missing .so or .cpython-312-x86_64-linux-gnu.so file. Error mentions ''undefined
    symbol'' or ''cannot open shared object file''.\n"}'
  affected_output: '{''root_cause'': "ANUGA''s C extensions (finite-volume kernels,
    mesh operations) were compiled for a different Python version or architecture.
    The .so files in the anuga package directory do not match th'
  detection: '{''root_cause'': "ANUGA''s C extensions (finite-volume kernels, mesh
    operations) were compiled for a different Python version or architecture. The
    .so files in the anuga package directory do not match th'
  fix: '{''action'': ''Reinstall ANUGA for the current Python: pip install --force-reinstall
    anuga. Verify with: python -c "import anuga; print(anuga.__file__)". The preflight_check.py
    script tests this automati'
- id: dt_anuga_017
  description: '{''description'': "ANUGA simulation hangs or produces a segfault during
    domain.evolve() when running in parallel (MPI). Or: sww_merge fails with ''file
    not found'' for one of the processor SWW fragments.\n"}'
  affected_output: '{''root_cause'': ''ANUGA parallel execution via mpirun creates
    per-processor domains and SWW files. If one MPI rank fails or is killed (e.g.,
    OOM), its SWW fragment is missing or truncated, and sww_merge'
  detection: '{''root_cause'': ''ANUGA parallel execution via mpirun creates per-processor
    domains and SWW files. If one MPI rank fails or is killed (e.g., OOM), its SWW
    fragment is missing or truncated, and sww_merge'
  fix: '{''action'': ''For the KI workflow, run ANUGA in serial (single process) which
    is the default in run_anuga.py. If parallel is needed, use: domain = anuga.distribute(domain)
    after setup, then mpirun -np N'
- id: dt_anuga_018
  description: '{''description'': ''DEM elevation values are all NaN or a single constant
    across the entire mesh. The terrain appears completely flat despite using a real
    DEM. Simulation produces uniform shallow flow everywhere.\n''}'
  affected_output: '{''root_cause'': ''The DEM GeoTIFF extent does not overlap with
    the ANUGA domain in local coordinates. run_anuga.py converts lat/lon to a local
    meter coordinate system centered at (0,0) and builds the do'
  detection: '{''root_cause'': ''The DEM GeoTIFF extent does not overlap with the
    ANUGA domain in local coordinates. run_anuga.py converts lat/lon to a local meter
    coordinate system centered at (0,0) and builds the do'
  fix: '{''action'': "Verify DEM coverage: the bounding box [lat-extent/2/111320,
    lon-extent/2/(111320*cos(lat))] must fall within the DEM file extent. Check with:
    gdalinfo china_dem_90m.tif | grep ''Upper Left\'
- id: dt_anuga_019
  description: '{''description'': ''Flood map is spatially displaced from the actual
    terrain by several hundred meters or more. Water flows in unexpected directions.
    Channels visible in the DEM do not appear in the correct location in ANUGA output.\n''}'
  affected_output: '{''root_cause'': ''x/y axis swap in the DEM-to-mesh coordinate
    transformation. The RegularGridInterpolator in load_dem_elevation takes (y, x)
    order for the points argument, but the ANUGA mesh uses (x, y)'
  detection: '{''root_cause'': ''x/y axis swap in the DEM-to-mesh coordinate transformation.
    The RegularGridInterpolator in load_dem_elevation takes (y, x) order for the points
    argument, but the ANUGA mesh uses (x, y)'
  fix: '{''action'': ''Verify the elevation_func: sample a few known (x,y) points
    and compare against the DEM. The current run_anuga.py interpolator uses pts =
    np.column_stack([y, x]) which is correct for Regula'
- id: dt_anuga_020
  description: '{''description'': ''ANUGA mesh has very few triangles in critical
    areas (channels, flood zones) but excessive refinement in unimportant areas. Simulation
    accuracy is poor despite high total triangle count.\n''}'
  affected_output: '{''root_cause'': "Uniform maximum_triangle_area used across the
    entire domain. ANUGA''s create_domain_from_regions supports interior_regions for
    local mesh refinement, but the default run_anuga.py applie'
  detection: '{''root_cause'': "Uniform maximum_triangle_area used across the entire
    domain. ANUGA''s create_domain_from_regions supports interior_regions for local
    mesh refinement, but the default run_anuga.py applie'
  fix: '{''action'': ''Use interior_regions parameter in create_domain_from_regions
    to specify polygons with smaller max_area for channels and flood zones. Example:
    interior_regions=[[channel_polygon, 100.0]] fo'
- id: dt_anuga_021
  description: '{''description'': ''Reported flood depth at a gauge location is 150-300m,
    which is physically impossible. The parse_anuga_output.py tool reports stage_m
    values that match terrain elevation rather than water depth.\n''}'
  affected_output: '{''root_cause'': "Stage (water surface elevation in m above datum)
    confused with depth (water thickness above bed). ANUGA stores ''stage'' in the
    SWW file, which equals bed_elevation + water_depth. For te'
  detection: '{''root_cause'': "Stage (water surface elevation in m above datum) confused
    with depth (water thickness above bed). ANUGA stores ''stage'' in the SWW file,
    which equals bed_elevation + water_depth. For te'
  fix: '{''action'': ''Always compute depth = stage - elevation. In parse_anuga_output.py,
    the extract_stage_at_gauge function correctly returns both stage and depth = max(stage
    - elevation, 0). Use the depth_m '
- id: dt_anuga_022
  description: '{''description'': ''Discharge extracted via get_flow_through_cross_section
    is zero or near-zero despite visible water flow in the domain. Or: discharge has
    the wrong sign (negative when inflow is expected).\n''}'
  affected_output: '{''root_cause'': "The cross-section polyline does not intersect
    any wet mesh triangles, or it is oriented perpendicular to the expected flow direction.
    ANUGA computes discharge as the integral of (uh, v'
  detection: '{''root_cause'': "The cross-section polyline does not intersect any
    wet mesh triangles, or it is oriented perpendicular to the expected flow direction.
    ANUGA computes discharge as the integral of (uh, v'
  fix: '{''action'': ''Ensure the cross-section polyline is perpendicular to the expected
    flow direction and fully within the mesh domain. The polyline coordinates must
    be in the same local coordinate system as '
unit_conversions: []
