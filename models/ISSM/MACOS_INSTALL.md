# ISSM native Apple Silicon installation

If this workspace already contains `issm-build-config.json`, its absolute prefixes identify genuine dependencies on this same host. After cloning the exact source, first run the trusted helper from `binaries/ISSM`: `python3 ../../ki/tools/build_issm_native.py .`. The helper validates these providers. A denied direct filesystem inspection outside the setup workspace does not establish missing dependencies; diagnose the helper result before attempting the cold dependency-build instructions below.

The official `2026.2` tag is pinned to `fe0867872156f8f77bdf7c6ce50cde301abe768e`. Its `configure.ac` still embeds `2026.1`: preserve this upstream discrepancy, identify by tag/commit, and do not fake the version string. The currently hosted official Apple Silicon ZIP contains2026.3 and must not silently replace this source.

The supported headless `--without-wrappers` route builds the actual ISSM native solver, its shared model libraries, and genuine solver dependencies. It does not install MATLAB/Python model workflow wrappers; the KI's Python simulation orchestration may need those later. Installation proof does not claim scientific workflow validation.

Build dependencies follow official `jenkins/mac-silicon-basic`: PETSc3.23.6, MPICH, METIS, ParMETIS, MUMPS5.7.3, ScaLAPACK2.2.2, Fortran BLAS/LAPACK, zlib, Triangle and M1QN3. On this Mac they use actual Homebrew MPICH4.3.1 plus gfortran, with no host links changed. Use `SSL_CERT_FILE=/opt/homebrew/etc/openssl@3/cert.pem` and `FI_PROVIDER=tcp FI_TCP_IFACE=en0`. PETSc archive SHA256: `07e0492c5c38d2fc5aa6dd981c450086f3b88f8834df11247a87d4becfb85c72`.

Build PETSc3.23.6 with real Python3.11 and `--with-make-np=2 --with-debugging=0 --with-valgrind=0 --with-x=0 --with-ssl=0 --with-pic=1 --with-cc=/opt/homebrew/bin/mpicc --with-cxx=/opt/homebrew/bin/mpicxx --with-fc=/opt/homebrew/bin/mpifort --download-fblaslapack=1 --download-metis=1 --download-mumps=1 --download-parmetis=1 --download-scalapack=1 --download-zlib=1 --prefix=ABSOLUTE_PREFIX`, then `make -j2` and `make install`. PETSc's with-ssl=0 disables its optional SSL feature; TLS download verification remains enabled.

Triangle uses upstream `externalpackages/triangle/install-mac.sh`. M1QN3 uses upstream `install-mac-static.sh`: the shared recipe mistakenly uses `-c`, producing an object misnamed as a dylib. Genuine static libraries avoid that upstream error. The now-removed M1QN3 archive is available from official historical source: `https://raw.githubusercontent.com/ISSMteam/ExternalPackages/c4fb623a60873302edf482a5d4dbb1f4dcce3186/m1qn3-3.3-distrib.tgz`, SHA256 `27c6a8f56a4080420c25ffb0743e3dece7c57cc1740776936f220b4ed28b89d9`. Stage this verified archive in the upstream package directory before invoking the static installer. Do not accept the HTML404 fetched by the original URL.

Provide workspace `issm-build-config.json` with absolute `petsc_prefix`, `triangle_prefix`, and `m1qn3_prefix` pointing to these genuine compiled dependencies. From `binaries/ISSM`, run `python3 ../../ki/tools/build_issm_native.py .`. The helper checks the exact source and PETSc version, then repairs six unconditional dependencies on the omitted wrapper library in Automake, guarded by source hashes. It changes no model code or equations. All real model libraries are rebuilt with two jobs.

Local receipt: actual arm64 `bin/issm.exe`39,864 bytes loads genuine `libISSMCore.dylib`5,487,456 bytes and `libISSMOverload.dylib`50,328 bytes. Empty-directory `--help` and `--version` reach native MPI/PETSc initialization then exit1 with `Usage error: missing execution directory`; no files are created. Source `src/c/classes/FemModel.cpp:77–85` initializes PETSc and processes arguments before `InitFromFiles`; `src/c/cores/ProcessArguments.cpp:14` gives this exact boundary. No simulation/input files are supplied. Independent DS verification remains pending.

Independent DS92 installation passed in624.9seconds. Retained source HEAD matches the exact2026.2 commit; the sole tracked change is the documented build-only Makefile.am conditional. Native solver SHA256 `fc33cd4688ea49e0e10c5816148dbb9d3ae8bde4c9f535e395208504ba91ab5b` (39,912bytes); real `libISSMCore.dylib`5,511,280bytes exposes669matching model/core symbols. Separate bounded probes again reached the exact missing-execution-directory boundary with nofiles. Source files defining the banner, native initialization, model constructor and argument boundary were byte-compared with pinned Git blobs. This validates headless software installation only.
