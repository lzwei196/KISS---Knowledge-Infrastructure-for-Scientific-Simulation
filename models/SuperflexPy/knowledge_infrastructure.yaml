# knowledge_infrastructure.yaml — the KI's manifest, PROJECTED from dag.yaml,
# the KI contents and the models DB (generate_ki_manifest.py, 2026-08-15).

package:
  name: SuperflexPy
  language: python
  implementation:
    id: superflexpy
    version: 1.3.2
    provenance: Python; SuperflexPy (Dal Molin / Fenicia / Kavetski, Eawag-WUR) component-based framework
      implementing the SUPERFLEX reservoir/lag/connector elements.
  scientific_reference_version: SUPERFLEX conceptual framework (Fenicia/Kavetski/Savenije 2011) as realized
    by SuperflexPy 1.3.x
  binary:
    path: KISSPATH_INTERNAL_NOT_SHIPPED/auto_dissect/_work/SuperflexPy/venv/bin/python
    type: work_dir
pipeline:
  stages:
  - s1_forcing_preparation
  - s2_parameter_setup
  - s3_model_assembly
  - s5_model_execution
  - s6_output_extraction
tools:
  count: 4
  files:
  - tools/convert_forcing.py
  - tools/convert_parameters.py
  - tools/parse_output.py
  - tools/run_superflexpy.py
inputs:
  forcing:
  - name: Precipitation (P)
    unit: mm/d
  - name: Potential evapotranspiration (PET)
    unit: mm/d
  - name: Temperature (T)
    unit: degC
  parameters:
  - name: x1 (GR4J production-store capacity)
    unit: mm
  - name: x2 (GR4J groundwater exchange coefficient)
    unit: mm/d
  - name: x3 (GR4J routing-store capacity)
    unit: mm
  - name: x4 (GR4J unit-hydrograph base time)
    unit: d
  - name: GR4J fixed shape exponents (alpha=2.0, beta=5.0, ni=4/9, gamma=5.0, omega=3.5)
    unit: '-'
  - name: HBV/HYMOD soil parameters (Smax, Ce, m, beta)
    unit: mm (Smax); - (Ce, m, beta)
  - name: Power/linear reservoir parameters (k, alpha)
    unit: 1/d (k); - (alpha)
  - name: Snow reservoir parameters (T0, m, k)
    unit: degC (T0); mm/degC/d (m); 1/d (k)
  - name: Catchment area
    unit: km2
  initial_conditions:
  - name: Reservoir initial storage (S0)
    unit: mm
  - name: Lag-element initial state
    unit: convolution array
  boundary_conditions:
  - name: HRU weights (Node)
    unit: fraction [0,1]
  - name: Network topology
    unit: '-'
  - name: ODE approximator choice
    unit: '-'
  - name: Root finder choice
    unit: '-'
  - name: Timestep (dt)
    unit: d
io_detail: 'full I/O contract incl. units/observability: docs/format_spec.yaml'
outputs:
  observable:
  - Q_sim
  - Network_Q
  - AET
  - S
  - F
  other: []
validation:
  tier: validated
  tier_justification: 'measured: NSE=0.823, KGE=0.850, R=0.924, 2 scorecard(s)'
  metrics:
    best_nse: 0.8231136715791392
    best_kge: 0.8503304849938911
    best_r: 0.9235162507230236
summary:
  total_tools: 4
  total_stage_docs: 5
  total_diagnostic_triplets: 21
  generated: '2026-08-22'
  generated_by: generate_ki_manifest.py (projection; regenerate any time)
