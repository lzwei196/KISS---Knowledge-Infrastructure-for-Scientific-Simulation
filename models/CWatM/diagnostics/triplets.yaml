# ============================================================================
# CWatM Diagnostic Triplets
# ============================================================================
# 18 diagnostic triplets covering 5 failure domains.
# Generated during CWatM knowledge dissection.
# ============================================================================

# --- Unit Conversion (HIGHEST PRIORITY — Silent Errors) ---

- id: dt_001
  stage: s1_forcing_preparation
  failure_domain: unit_conversion
  symptom:
    description: "Extreme flooding or unrealistically high discharge. River discharge 100-1000× too high."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "precipitation_coversion factor wrong — input precip units don't match conversion factor"
    explanation: >
      CWatM multiplies precipitation input by precipitation_coversion to get m/day.
      Default is 86.4 which converts kg/m²/s to m/day (= 86400 s/day ÷ 1000 mm/m).
      If input is already in mm/day and conversion is 86.4, precipitation is 86.4× too high.
      If input is in m/day and conversion is 86.4, precipitation is 86.4× too high.
    affected_components: [readmeteo, settings.ini]
  remedy:
    action: "Match precipitation_coversion to actual input units"
    tool_to_run: convert_forcing_to_cwatm
    manual_steps:
      - "Check actual units in forcing NetCDF: ncdump -h precipitation.nc"
      - "kg/m²/s: use 86.4 (default)"
      - "m/day: use 1.0"
      - "mm/day: use 0.001"
      - "mm/hr: use 0.024"
    prevention: "validate_converted_data() checks precipitation magnitude"
  severity: silent
  cross_references: [dt_002]

- id: dt_002
  stage: s1_forcing_preparation
  failure_domain: unit_conversion
  symptom:
    description: "Zero or near-zero discharge. No runoff generated despite precipitation."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "Precipitation too low — conversion factor divides instead of multiplying, or wrong scale"
    explanation: >
      If precipitation_coversion = 1.0 but input is in kg/m²/s, the internal precipitation
      is ~0.00001 m/day instead of ~0.001 m/day. All water evaporates or infiltrates,
      generating no runoff. No error message is raised.
    affected_components: [readmeteo, settings.ini]
  remedy:
    action: "Check precipitation internal values: should be ~0.001-0.01 m/day for most climates"
    tool_to_run: convert_forcing_to_cwatm
    manual_steps:
      - "Add temporary output: OUT_MAP_Daily = precipitation"
      - "Check output: daily precipitation should be 0-0.1 m/day (0-100 mm/day)"
    prevention: "Output precipitation map for first month and check magnitude"
  severity: silent
  cross_references: [dt_001]

- id: dt_003
  stage: s1_forcing_preparation
  failure_domain: unit_conversion
  symptom:
    description: "Snow never melts, or all precipitation is snow/rain regardless of season. Wrong snowpack dynamics."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "TemperatureInKelvin flag doesn't match input data units"
    explanation: >
      If TemperatureInKelvin = True but input is in Celsius (e.g., 20°C), CWatM treats
      20 as 20 K = -253°C. All precipitation becomes snow, nothing melts.
      Conversely, if flag = False but input is Kelvin (293 K), CWatM adds 273.15,
      making temperature 566°C — everything melts instantly, extreme ET.
    affected_components: [readmeteo, snow_frost, evaporationPot]
  remedy:
    action: "Check temperature values in forcing NetCDF and set flag correctly"
    tool_to_run: convert_forcing_to_cwatm
    manual_steps:
      - "ncdump -v tavg precipitation.nc | head -20"
      - "If values > 200: data is Kelvin, set TemperatureInKelvin = True"
      - "If values < 100: data is Celsius, set TemperatureInKelvin = False"
    prevention: "validate_converted_data() checks temperature range"
  severity: silent
  cross_references: []

- id: dt_004
  stage: s2_static_data
  failure_domain: unit_conversion
  symptom:
    description: "All water drains instantly or soil stays waterlogged. Unrealistic soil moisture dynamics."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "KSat units wrong — CWatM expects cm/day, input may be mm/hr or m/s"
    explanation: >
      CWatM reads KSat in cm/day. SoilGrids provides mm/hr (factor 2.4 to cm/day).
      Some datasets provide m/s (factor 8,640,000 to cm/day). Using mm/hr values
      directly (without ×2.4) makes KSat ~2.4× too low. Using m/s values without
      conversion makes KSat millions of times wrong.
    affected_components: [soil, convert_soil_to_cwatm]
  remedy:
    action: "Convert KSat to cm/day before use"
    tool_to_run: convert_soil_to_cwatm
    manual_steps:
      - "HWSD: already cm/day (no conversion)"
      - "SoilGrids: cm/day = mm/hr × 2.4"
      - "m/s: cm/day = m/s × 8640000"
      - "Typical range: Sand 100-700, Loam 5-50, Clay 0.5-5 cm/day"
    prevention: "validate_outputs() checks KSat range"
  severity: silent
  cross_references: [dt_005]

- id: dt_005
  stage: s2_static_data
  failure_domain: unit_conversion
  symptom:
    description: "Soil water retention curve is wrong. Field capacity or wilting point unrealistic."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "van Genuchten alpha in wrong units — 1/m instead of 1/cm"
    explanation: >
      CWatM expects alpha in 1/cm. Some databases provide 1/m or 1/kPa.
      Using 1/m directly makes alpha 100× too large, dramatically changing the
      soil water retention curve. The soil will appear much sandier than it is.
    affected_components: [soil, convert_soil_to_cwatm]
  remedy:
    action: "Ensure alpha is in 1/cm"
    tool_to_run: convert_soil_to_cwatm
    manual_steps:
      - "If source is 1/m: divide by 100"
      - "Typical range: 0.001-0.5 1/cm"
      - "Sand: ~0.145, Loam: ~0.036, Clay: ~0.008"
    prevention: "validate_outputs() checks alpha range"
  severity: silent
  cross_references: [dt_004]

- id: dt_006
  stage: s1_forcing_preparation
  failure_domain: unit_conversion
  symptom:
    description: "ET is extremely high or near zero. Penman-Monteith produces unrealistic values."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "Surface pressure in hPa instead of Pa, or radiation in wrong units"
    explanation: >
      Penman-Monteith requires pressure in Pa and radiation in W/m². If pressure is
      provided in hPa (1013 instead of 101300), the psychrometric constant is 100× wrong.
      If radiation is in MJ/m²/day instead of W/m², it's ~11.6× wrong.
    affected_components: [evaporationPot, readmeteo]
  remedy:
    action: "Check units of all Penman-Monteith inputs"
    manual_steps:
      - "Pressure: should be 50000-110000 Pa (not 500-1100 hPa)"
      - "Shortwave: should be 0-500 W/m² (not 0-40 MJ/m²/day)"
      - "Wind: should be 0-20 m/s (not 0-70 km/hr)"
    prevention: "validate_converted_data() checks all variable ranges"
  severity: silent
  cross_references: [dt_003]

# --- Configuration Errors ---

- id: dt_007
  stage: s0_configuration
  failure_domain: configuration
  symptom:
    description: "CWatM crashes at startup with 'option not found' or 'key not found' error."
    error_pattern: "CWATMError"
    detection_method: log_match
  diagnosis:
    root_cause: "Settings file section or key name typo, or missing required section"
    explanation: >
      CWatM's ExtParser is case-sensitive for keys. A typo in a key name (e.g.,
      'PrecipitationMap' instead of 'PrecipitationMaps') causes a missing key error.
      The error message includes a 'closest match' suggestion using difflib.
    affected_components: [configuration.py]
  remedy:
    action: "Check the error message for suggested closest match"
    manual_steps:
      - "Compare your settings file against the template"
      - "Check for typos in section names and key names"
      - "Ensure all required sections exist"
    prevention: "Use the template settings file as a starting point"
  severity: fatal
  cross_references: []

- id: dt_008
  stage: s0_configuration
  failure_domain: configuration
  symptom:
    description: "Date parsing error or 'StepEnd before StepStart' error."
    error_pattern: "date"
    detection_method: log_match
  diagnosis:
    root_cause: "Date format wrong — CWatM expects DD/MM/YYYY, not YYYY-MM-DD"
    explanation: >
      CWatM date parsing expects DD/MM/YYYY format. Using ISO format (YYYY-MM-DD)
      or US format (MM/DD/YYYY) causes either a parse error or a silently wrong date.
      For example, 2010-01-31 is interpreted as DD=2010, which fails.
    affected_components: [timestep.py, configuration.py]
  remedy:
    action: "Use DD/MM/YYYY format for all dates"
    manual_steps:
      - "StepStart = 01/01/1990  (not 1990-01-01)"
      - "SpinUp = 01/01/1995"
      - "StepEnd = 31/12/2010"
    prevention: "Always use DD/MM/YYYY format"
  severity: fatal
  cross_references: []

- id: dt_009
  stage: s0_configuration
  failure_domain: configuration
  symptom:
    description: "precipitation_coversion spelled as 'precipitation_conversion' — silently ignored."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "CWatM code has a typo in the key name: 'precipitation_coversion' (missing 'n')"
    explanation: >
      The settings key is 'precipitation_coversion' (sic), not 'precipitation_conversion'.
      If you use the correct English spelling, CWatM won't find the key and will use
      the default value (86.4), which may be wrong for your input data. No error is raised.
    affected_components: [readmeteo, configuration.py]
  remedy:
    action: "Use the misspelled key: precipitation_coversion (without the 'n')"
    manual_steps:
      - "Search settings file for 'conversion' and replace with 'coversion'"
    prevention: "Copy the key name exactly from the template"
  severity: silent
  cross_references: [dt_001, dt_002]

# --- Spatial Data Errors ---

- id: dt_010
  stage: s2_static_data
  failure_domain: spatial_data
  symptom:
    description: "Water accumulates in random cells. Discharge at outlet is near zero. Disconnected drainage."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "LDD coding convention wrong — ArcGIS D8 instead of PCRaster convention"
    explanation: >
      CWatM uses PCRaster LDD coding (1=SW, 2=S, 3=SE, ..., 9=NE, 5=pit).
      ArcGIS D8 uses power-of-2 encoding (1=E, 2=SE, 4=S, ..., 128=NE).
      Using ArcGIS LDD without conversion creates a completely wrong drainage network.
    affected_components: [routing_kinematic, routing_sub]
  remedy:
    action: "Convert ArcGIS D8 to PCRaster LDD convention"
    manual_steps:
      - "ArcGIS mapping: 1→6, 2→3, 4→2, 8→1, 16→4, 32→7, 64→8, 128→9"
      - "Or use HydroSHEDS flow direction with proper conversion"
      - "Verify: pit cells (value=5) should be at basin outlet only"
    prevention: "Validate LDD with upstream area calculation"
  severity: fatal
  cross_references: []

- id: dt_011
  stage: s2_static_data
  failure_domain: spatial_data
  symptom:
    description: "Division by zero error in routing. Model crashes during kinematic wave computation."
    error_pattern: "division by zero"
    detection_method: log_match
  diagnosis:
    root_cause: "Channel gradient contains zero values"
    explanation: >
      The kinematic wave equation requires non-zero channel gradient. Flat areas
      (gradient=0) cause division by zero in the routing solver. CWatM expects
      a minimum gradient of chanGradMin (default 0.0001).
    affected_components: [routing_kinematic, t5.cpp]
  remedy:
    action: "Enforce minimum channel gradient"
    manual_steps:
      - "chanGrad = max(chanGrad, 0.0001)"
      - "Or set chanGradMin = 0.0001 in settings [ROUTING] section"
    prevention: "Always clip gradient to minimum value"
  severity: fatal
  cross_references: []

- id: dt_012
  stage: s2_static_data
  failure_domain: spatial_data
  symptom:
    description: "Grid resolution mismatch error. Meteo data doesn't overlay correctly on maps."
    error_pattern: "shape mismatch"
    detection_method: log_match
  diagnosis:
    root_cause: "Meteorological forcing grid doesn't match model grid resolution"
    explanation: >
      CWatM can handle mismatched resolutions using interpolation (set
      meteomapsscale = False in code), but the grids must share the same
      coordinate reference system. A mismatch in array shapes causes errors.
    affected_components: [readmeteo, data_handling]
  remedy:
    action: "Ensure forcing data covers the model domain, let CWatM interpolate"
    manual_steps:
      - "Check both grids use the same CRS (WGS84)"
      - "Forcing grid should be same resolution or coarser"
      - "If different resolution, CWatM interpolates automatically"
    prevention: "Pre-process forcing to match model grid"
  severity: fatal
  cross_references: []

# --- Runtime Errors ---

- id: dt_013
  stage: s3_execution
  failure_domain: runtime
  symptom:
    description: "Routing is extremely slow. Model takes hours for a small basin."
    error_pattern: ""
    detection_method: performance
  diagnosis:
    root_cause: "C++ shared library (t5_linux.so) failed to load — falling back to Python routing"
    explanation: >
      CWatM's kinematic wave routing can use a C++ library for ~100× speedup.
      If the library fails to load (missing dependencies, wrong architecture),
      CWatM silently falls back to pure Python, which is dramatically slower.
    affected_components: [routing_kinematic, t5.cpp]
  remedy:
    action: "Check C++ library loading"
    manual_steps:
      - "python -c \"import ctypes; ctypes.cdll.LoadLibrary('t5_linux.so')\""
      - "If error: recompile from t5.cpp with g++ -shared -fPIC -O2 -o t5_linux.so t5.cpp"
      - "Check: ldd t5_linux.so for missing dependencies"
    prevention: "Test library loading before model run"
  severity: degraded
  cross_references: []

- id: dt_014
  stage: s3_execution
  failure_domain: runtime
  symptom:
    description: "Out of memory error. Model crashes during initialization or first timestep."
    error_pattern: "MemoryError"
    detection_method: log_match
  diagnosis:
    root_cause: "Domain too large for available RAM"
    explanation: >
      CWatM loads all static data and several timesteps of forcing into memory.
      A 5-arcmin global domain requires ~100+ GB RAM. Even 30-arcmin global
      needs 8-16 GB.
    affected_components: [data_handling, readmeteo]
  remedy:
    action: "Reduce domain size or increase RAM"
    manual_steps:
      - "Use a smaller MaskMap (basin-level instead of global)"
      - "Use 30-arcmin instead of 5-arcmin resolution"
      - "Increase available RAM or use a machine with more memory"
    prevention: "Estimate memory: ~500 bytes per cell per variable"
  severity: fatal
  cross_references: []

- id: dt_015
  stage: s3_execution
  failure_domain: runtime
  symptom:
    description: "NaN values propagate through all outputs. Discharge is NaN everywhere."
    error_pattern: "nan"
    detection_method: value_comparison
  diagnosis:
    root_cause: "NaN in input forcing data or soil parameters"
    explanation: >
      CWatM does not automatically handle missing values in input data. A single
      NaN in forcing (e.g., one missing precipitation value) propagates through
      all calculations, eventually making all outputs NaN.
    affected_components: [readmeteo, data_handling]
  remedy:
    action: "Pre-fill or interpolate missing values in all input data"
    manual_steps:
      - "Check for NaN: python -c \"import netCDF4; ds=netCDF4.Dataset('file.nc'); print(np.isnan(ds.variables['var'][:]).sum())\""
      - "Fill gaps with temporal interpolation or climatological means"
      - "For soil: fill with nearest-neighbor or regional defaults"
    prevention: "Always check for NaN before running CWatM"
  severity: silent
  cross_references: []

# --- Output Interpretation ---

- id: dt_016
  stage: s4_output_analysis
  failure_domain: output_interpretation
  symptom:
    description: "Discharge unrealistically high for first few years, then stabilizes."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "Insufficient spinup period. Initial conditions not equilibrated."
    explanation: >
      CWatM starts with default initial conditions (all storage = 0 or default values).
      The model needs 3-5 years to equilibrate groundwater and soil moisture storage.
      During spinup, discharge may be too high (if starting dry) or too low (if starting wet).
    affected_components: [initcondition, groundwater]
  remedy:
    action: "Increase spinup period to 3-5 years"
    manual_steps:
      - "Set SpinUp date at least 3 years after StepStart"
      - "Or use saved initial conditions from a previous long run"
      - "Exclude spinup period from all metric calculations"
    prevention: "Always use ≥ 3 year spinup, ≥ 5 for arid regions"
  severity: degraded
  cross_references: []

- id: dt_017
  stage: s4_output_analysis
  failure_domain: output_interpretation
  symptom:
    description: "Discharge timing is correct but magnitude is systematically too high or low (PBIAS > 30%)."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "ET calibration parameter (crop_correct) needs adjustment"
    explanation: >
      crop_correct scales potential evapotranspiration. If too low, too little water
      evaporates and too much becomes runoff (positive PBIAS). If too high, too much
      evaporates and too little reaches the river (negative PBIAS).
    affected_components: [evaporationPot, calibration]
  remedy:
    action: "Adjust crop_correct parameter"
    manual_steps:
      - "PBIAS > 0 (too much discharge): increase crop_correct"
      - "PBIAS < 0 (too little discharge): decrease crop_correct"
      - "Typical range: 0.8 to 1.5"
      - "Also check soildepth_factor if crop_correct alone is insufficient"
    prevention: "Calibrate crop_correct first in any calibration sequence"
  severity: degraded
  cross_references: [dt_018]

- id: dt_018
  stage: s4_output_analysis
  failure_domain: output_interpretation
  symptom:
    description: "Hydrograph peaks are too sharp or too flat. Recession limb is wrong."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "Runoff partitioning or groundwater recession parameters need calibration"
    explanation: >
      arnoBeta_add controls the fraction of precipitation becoming direct runoff
      (higher = more surface runoff = sharper peaks). recessionCoeff_factor controls
      how fast groundwater releases water (higher = slower release = flatter recession).
    affected_components: [soil, groundwater, calibration]
  remedy:
    action: "Adjust arnoBeta_add and recessionCoeff_factor"
    manual_steps:
      - "Peaks too sharp: decrease arnoBeta_add, increase recessionCoeff_factor"
      - "Peaks too flat: increase arnoBeta_add, decrease recessionCoeff_factor"
      - "arnoBeta_add range: 0.01 to 1.0"
      - "recessionCoeff_factor range: 1.0 to 10.0"
    prevention: "Calibrate after crop_correct and soildepth_factor are set"
  severity: degraded
  cross_references: [dt_017]

- id: cwatm_dt_011
  stage: s3_model_execution
  failure_domain: model_internals
  symptom:
    description: "CWatM aborts as soon as runoff concentration initialises, when tanslope is a scalar in settings.ini."
    error_pattern: "ctypes.ArgumentError"
    detection_method: exception
  diagnosis:
    root_cause: "runoff_concentration.py computes tpeak from tanslope; a scalar tanslope makes tpeak a Python float, but the C++ routing helper requires an ndarray over the compressed active cells."
    explanation: >
      `tpeak = 0.5 + 0.6 * 50000 / (1440*60*sqrt(tanslope))` inherits the type of
      tanslope. With a tanslope MAP it is an ndarray and everything works; with a
      tanslope SCALAR it stays a float and the ctypes call rejects it. The 2026-05
      real case hit this and worked around it by running a patched copy of the repo
      OUTSIDE the KI, leaving the canonical source broken.
    affected_components: [runoff_concentration, routing]
  remedy:
    action: "Broadcast tpeak onto the active-cell array"
    manual_steps:
      - "In cwatm/hydrological_modules/runoff_concentration.py, after the tpeak assignment add: tpeak = globals.inZero + tpeak"
      - "Already applied to source/repo in this KI (2026-07-09)."
    prevention: "Never patch a copy of the model outside the KI; fix source/repo so the next agent inherits the fix."
  severity: fatal
  cross_references: []

- id: cwatm_dt_012
  stage: s1_forcing_preparation
  failure_domain: input_data
  symptom:
    description: "Shape mismatch between meteo and static maps, or the forcing is one day shorter than tmin/tmax."
    error_pattern: "shape mismatch"
    detection_method: value_comparison
  diagnosis:
    root_cause: "Two independent defects: (1) convert_forcing_to_cwatm used to emit the forcing at its NATIVE resolution (0.1 deg for CMFD) while the static maps are 0.25/0.5 deg; (2) the time filter compared full datetimes, and CMFD daily timestamps are not at midnight, so the final day of --end_date was silently dropped."
    explanation: >
      CWatM requires every input NetCDF to share the clone-map grid exactly.
      Use --target_res equal to the static-map --res; the tool block-means and
      orders latitudes north-first. The time filter now compares .date().
    affected_components: [readmeteo, cwatm_initial]
  remedy:
    action: "Regenerate forcing with --target_res, then verify the grids"
    manual_steps:
      - "python tools/convert_forcing_to_cwatm.py ... --target_res <same as static --res>"
      - "Assert lat/lon of every forcing, soil and ancillary NetCDF equals static/MaskMap.nc"
      - "Assert all forcing variables have the SAME number of timesteps"
    prevention: "Build static maps first; they define the grid. Never resample the static maps to the forcing."
  severity: fatal
  cross_references: [cwatm_dt_013]

- id: cwatm_dt_013
  stage: s3_model_execution
  failure_domain: model_internals
  symptom:
    description: "run_cwatm_wrapper reports status=timeout after exactly 3600 s, or 'can't open file .../source/repo/source/repo/run_cwatm.py'."
    error_pattern: "timeout|can't open file"
    detection_method: exception
  diagnosis:
    root_cause: "The wrapper hard-coded a 1-hour subprocess timeout, and it joined a RELATIVE --cwatm_dir onto run_cwatm.py while also passing cwd=cwatm_dir, doubling the path."
    explanation: >
      A 0.5-degree multi-decade basin run takes hours (Jiujiang: ~2.4 s per
      simulated day over 570 active cells). Both defects are fixed: timeout is now
      an optional --timeout (default: no limit) and paths are absolutised.
    affected_components: [run_cwatm_wrapper]
  remedy:
    action: "Use the patched wrapper; pass --flags=-q (the '=' form) and an absolute or relative --cwatm_dir"
    manual_steps:
      - "argparse treats '--flags -q' as an option, not a value: always write --flags=-q"
      - "Long runs: launch via kdt_detached_run.py, not inline"
    prevention: "Estimate runtime from a 1-2 month smoke run before committing to the full period."
  severity: fatal
  cross_references: [cwatm_dt_012]

- id: cwatm_dt_014
  stage: s2_static_data_preparation
  failure_domain: input_data
  symptom:
    description: "Capillary rise behaves oddly, or the first 10-day period of each year has zero interception capacity."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "build_cwatm_ancillary fabricated relativeElevation (dzRel) from a hand-written factor list times ElevationStD, and its monthly->10-day interpolator never assigned index 0."
    explanation: >
      capillarRise.py reads dzRel[0..11] directly as the sub-grid elevation
      distribution above the cell minimum at cumulative area fractions
      1,5,10,20..100 %. These are measurable: build_cwatm_static.py now writes the
      TRUE percentiles from MERIT-Hydro elv, and build_cwatm_ancillary copies them.
      The 10-day interpolator now wraps December around so index 0 is real.
    affected_components: [capillarRise, interception, evaporation]
  remedy:
    action: "Re-run build_cwatm_static.py, then build_cwatm_ancillary.py"
    manual_steps:
      - "Check relativeElevation.nc has 12 dzRel variables and dzRel0100 >> dzRel0001"
      - "Check interceptCap*_10days.nc has a non-zero value at time index 0"
    prevention: "Prefer a measured field over a fabricated profile whenever the DEM is already on disk."
  severity: degraded
  cross_references: []

- id: cwatm_dt_015
  stage: s1_forcing_preparation
  failure_domain: input_data
  symptom:
    description: "Forcing conversion takes ~11 minutes per variable for a 2-month window."
    error_pattern: ""
    detection_method: value_comparison
  diagnosis:
    root_cause: "The reader globbed every CMFD file for a variable (74 yearly files, or 888 monthly files for the 3-hourly archive) and opened each one just to read its `time` axis."
    explanation: >
      The year is in the file NAME. Prefilter on a (19|20)\d{2} regex -- anchored so
      the product-version stem V0200 is not misread as a year -- before opening
      anything. 2-month CMFD conversion drops from 11 min to 20 s.
    affected_components: [convert_forcing_to_cwatm]
  remedy:
    action: "Use the patched reader (filename year prefilter + contiguous spatial slices)"
    manual_steps:
      - "Read the spatial subset in the netCDF4 call, not after: ds[var][tidx, lat_slice, lon_slice]"
    prevention: "Same class of bug as HBV's _load_cmfd year-glob: never glob *YYYY* against a stem containing digits."
  severity: degraded
  cross_references: [cwatm_dt_012]

- id: cwatm_dt_016
  stage: s3_model_execution
  failure_domain: environment
  symptom:
    description: "ImportError from osgeo/GDAL about a TLS static block when importing CWatM."
    error_pattern: "cannot allocate memory in static TLS block"
    detection_method: exception
  diagnosis:
    root_cause: "GDAL's shared object is loaded after libstdc++ has exhausted the static TLS budget in this Python environment."
    explanation: "Environment defect, not a CWatM defect. Preloading libstdc++ reserves the block first."
    affected_components: [environment]
  remedy:
    action: "Preload libstdc++"
    manual_steps:
      - "export LD_PRELOAD=/lib/x86_64-linux-gnu/libstdc++.so.6"
      - "Use KISSPATH_PYTHON_ENV/bin/python"
    prevention: "Set LD_PRELOAD in the runner's env dict, not interactively."
  severity: fatal
  cross_references: []

- id: cwatm_dt_017
  stage: s5_calibration
  failure_domain: parameterisation
  symptom:
    description: >-
      Uncalibrated discharge has good correlation but poor NSE (the burek2020 signature),
      AND the seasonal cycle is a quarter-cycle late: simulated autumn/winter flow far
      exceeds observed while spring flow falls short. Raising crop_correct or arnoBeta_add
      barely moves NSE.
    error_pattern: "baseflow fraction > 0.6 with baseflow climatology peaking 3-4 months after precipitation"
    detection_method: diagnostic
  diagnosis:
    root_cause: >-
      recessionCoeff_factor is a DIVISOR of recessionCoeff (groundwater.py:72-73:
      recessionCoeff = 1/recessionCoeff * factor; recessionCoeff = 1/recessionCoeff),
      so effective recessionCoeff = recessionCoeff / recessionCoeff_factor. With
      recessionCoeff = 0.02 and factor = 1.0 the groundwater residence time is 50 days,
      the store swallows most of the runoff and phase-lags the hydrograph.
    explanation: >-
      NSE = 2*a*r - a^2 - b^2, so max NSE over all bias/variance corrections is r^2.
      A phase-lagged store caps r, which caps NSE. Measured at Jiujiang (Yangtze, 1.5M km2,
      2010-2023): BFI 0.75, baseflow peaks in October vs precipitation in June, r = 0.729
      => NSE ceiling 0.531, actual NSE 0.184. Compute r^2 BEFORE targeting an NSE.
    affected_components: [groundwater, calibration]
  remedy:
    action: "Pin recessionCoeff = 1.0 and calibrate recessionCoeff_factor as the residence time in days"
    manual_steps:
      - "In [GROUNDWATER] set: recessionCoeff  = 1.0"
      - "Calibrate recessionCoeff_factor over [2, 40] -- it now equals tau_gw in days."
      - "Verify: OUT_Map_MonthAvg = runoff, baseflow; area-weighted BFI should land near 0.3-0.45."
    prevention: >-
      SKILL.md's documented 1.0-10.0 range only makes baseflow slower. Never search it
      without first checking the sign of the parameter in groundwater.py.
  severity: degraded
  cross_references: [cwatm_dt_018]

- id: cwatm_dt_018
  stage: s4_output_analysis
  failure_domain: tooling
  symptom:
    description: "parse_cwatm_output.py cannot find the gauge discharge series even though the run succeeded."
    error_pattern: "ERROR: Cannot find output file containing variable 'discharge'"
    detection_method: exception
  diagnosis:
    root_cause: >-
      CWatM writes gauge time series configured by OUT_TSS_Daily as <var>_daily.CSV, not NetCDF.
      The parser only globbed *.nc, so the one file holding the point series it exists to extract
      was invisible. A <var>_daily.nc appears only when OUT_MAP_Daily is also set.
    explanation: "TSS layout: 4 header rows (Timeseries.., xloc.., yloc.., Date,G1..), then DD/MM/YYYY,value."
    affected_components: [tools/parse_cwatm_output.py]
  remedy:
    action: "Fixed in place: parse_cwatm_output.py now prefers the TSS CSV"
    manual_steps:
      - "find_tss_csv() locates <variable>_<agg>.csv; extract_tss_csv() reads it with --gauge_index."
    prevention: "Do not infer 'the run produced no gauge output' from the absence of a NetCDF."
  severity: degraded
  cross_references: [cwatm_dt_017]

- id: cwatm_dt_019
  stage: s3_model_execution
  failure_domain: tooling
  symptom:
    description: "run_cwatm_wrapper.py reports run_cwatm.py missing at a doubled path, or dies on the GDAL TLS ImportError."
    error_pattern: "source/repo/source/repo/run_cwatm.py|cannot allocate memory in static TLS block"
    detection_method: exception
  diagnosis:
    root_cause: >-
      run_model() launched the subprocess with cwd=cwatm_dir but passed cwatm_dir and
      settings_path through unchanged, so any RELATIVE path was resolved a second time
      against cwatm_dir. Separately it inherited the parent env, so callers had to remember
      the LD_PRELOAD from cwatm_dt_016 by hand.
    affected_components: [tools/run_cwatm_wrapper.py]
  remedy:
    action: "Fixed in place"
    manual_steps:
      - "run_model() now abspath()s cwatm_dir and settings_path."
      - "run_model() injects LD_PRELOAD=/lib/x86_64-linux-gnu/libstdc++.so.6 into the child env."
    prevention: "Any subprocess launched with an explicit cwd must receive absolute paths."
  severity: fatal
  cross_references: [cwatm_dt_016]

- id: cwatm_dt_020
  stage: s0_configuration
  failure_domain: configuration
  symptom:
    description: "An irrigated basin simulates too much discharge; unmetDemand is all zero; lake parameters have no effect."
    error_pattern: "includeIrrigation = True with includeWaterDemand = False"
    detection_method: diagnostic
  diagnosis:
    root_cause: >-
      includeIrrigation only controls the LAND SURFACE: with it True, irrPaddy/irrNonPaddy stay
      as distinct land-cover types with their own crop coefficients and paddy topwater
      (landcoverType.py:785, 855). The ABSTRACTION of irrigation water from rivers and
      groundwater lives in waterdemand_module.dynamic(), which is gated on includeWaterDemand.
      Likewise lakeAFactor / normalStorageLimit / lakeEvaFactor are inert while
      includeWaterBodies = False -- 3 of the 11 documented calibration parameters do nothing.
    affected_components: [water_demand, lakes_reservoirs]
  remedy:
    action: "State the coupling explicitly in the settings header, or supply the demand stack"
    manual_steps:
      - "Water demand needs domestic/industrial/livestock demand NetCDFs; none are on this server as of 2026-07-10."
      - "Water bodies need lakesResID/lakesResType/lakesResDis/lakesResArea NetCDFs derived from HydroLAKES."
      - "HydroLAKES_polys_v10.shp IS on disk at KISSPATH_DATA/lakes/ but NO KI tool builds the 4 NetCDFs."
    prevention: >-
      Before calibrating, list which of the 11 parameters are actually live under the current
      option switches. Do not report an inert parameter as calibrated.
  severity: degraded
  cross_references: [cwatm_dt_017]
