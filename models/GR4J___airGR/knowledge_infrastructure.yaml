# knowledge_infrastructure.yaml — the KI's manifest, PROJECTED from dag.yaml,
# the KI contents and the models DB (generate_ki_manifest.py, 2026-08-15).

package:
  name: GR4J___airGR
  language: fortran
  implementation:
    id: airgr-gr4j
    version: airGR 1.7.8
    provenance: Fortran 90 water-balance core (frun_GR4J.f90) wrapped by the R package airGR (INRAE /
      HYCAR, Antony).
  scientific_reference_version: GR4J (Perrin, Michel & Andreassian 2003, J. Hydrology 279:275-289)
  binary:
    path: KISSPATH_INTERNAL_NOT_SHIPPED/auto_dissect/_work/GR4J___airGR/source/repo/src/airGR.so
    type: work_dir
pipeline:
  stages:
  - s1_forcing_preparation
  - s2_catchment_parameters
  - s3_model_execution
  - s4_calibration
  - s5_output_analysis
tools:
  count: 4
  files:
  - tools/convert_catchment_params.py
  - tools/convert_forcing_to_gr4j.py
  - tools/parse_gr4j_output.py
  - tools/run_gr4j.py
inputs:
  forcing:
  - name: Precip
    unit: mm/day
  - name: PotEvap
    unit: mm/day
  - name: DatesR
    unit: POSIXt
  parameters:
  - name: X1 — production store capacity
    unit: mm
  - name: X2 — inter-catchment exchange coefficient
    unit: mm/day
  - name: X3 — routing store capacity
    unit: mm
  - name: X4 — unit hydrograph time constant
    unit: day
  initial_conditions:
  - name: Production store initial level
    unit: mm
  - name: Routing store initial level
    unit: mm
  - name: UH1 / UH2 initial ordinate storages
    unit: mm
  boundary_conditions:
  - name: Warm-up period selection
    unit: timesteps (days)
  - name: Optional CemaNeige snow coupling
    unit: switch
io_detail: 'full I/O contract incl. units/observability: docs/format_spec.yaml'
outputs:
  observable:
  - Qsim
  - AE
  - Prod
  - AExch
  other: []
validation:
  tier: validated
  tier_justification: 'measured: NSE=0.800, KGE=0.754, R=0.899, 2 scorecard(s)'
  metrics:
    best_nse: 0.8003
    best_kge: 0.7538
    best_r: 0.899
summary:
  total_tools: 4
  total_stage_docs: 5
  total_diagnostic_triplets: 18
  generated: '2026-08-15'
  generated_by: generate_ki_manifest.py (projection; regenerate any time)
