# knowledge_infrastructure.yaml — the KI's manifest, PROJECTED from dag.yaml,
# the KI contents and the models DB (generate_ki_manifest.py, 2026-08-15).

package:
  name: RAPID
  language: fortran
  implementation:
    id: rapid-fortran-petsc
    version: main branch (2024-era; release lineage includes v1.8.0)
    provenance: Fortran 90; Cedric H. David (NASA JPL); PETSc/TAO sparse linear algebra with CF-NetCDF
      I/O.
  scientific_reference_version: Matrix-based Muskingum river routing (David et al. 2011, JHM); RAPID main-branch
    F90 source (2024-era)
  binary:
    path: KISSPATH_INTERNAL_NOT_SHIPPED/auto_dissect/_work/RAPID/source/repo/src/rapid
    type: work_dir
pipeline:
  stages:
  - s1_connectivity
  - s2_lateral_inflow
  - s3_parameters
  - s4_namelist
  - s5_execution
  - s6_output_analysis
tools:
  count: 6
  files:
  - tools/build_connectivity.py
  - tools/convert_lsm_to_vlat.py
  - tools/generate_muskingum_params.py
  - tools/generate_namelist.py
  - tools/parse_rapid_output.py
  - tools/run_rapid.py
inputs:
  forcing:
  - name: Vlat
    unit: m3 (accumulated volume over ZS_TauR, NOT a rate)
  parameters:
  - name: k
    unit: seconds
  - name: x
    unit: dimensionless
  - name: kfac / xfac
    unit: dimensionless
  - name: rapid_connect
    unit: dimensionless (reach IDs / indices)
  - name: riv_bas_id
    unit: dimensionless (reach IDs)
  initial_conditions:
  - name: Qinit
    unit: m3/s
  boundary_conditions:
  - name: IS_opt_run
    unit: enum
  - name: IS_opt_routing
    unit: enum
  - name: ZS_TauM / ZS_dtM / ZS_TauR / ZS_dtR
    unit: seconds
  - name: Qobs (+ obs_tot_id / obs_use_id)
    unit: m3/s
  - name: Qfor / dam IDs
    unit: m3/s (Qfor); dimensionless IDs (dam)
  - name: BS_opt_V / BS_opt_Qfinal / BS_opt_Qinit / BS_opt_influence
    unit: boolean
io_detail: 'full I/O contract incl. units/observability: docs/format_spec.yaml'
outputs:
  observable:
  - Qout
  - V
  other:
  - Qfinal
validation:
  tier: validated
  tier_justification: 'measured: NSE=0.749, KGE=0.790, R=0.928, 2 scorecard(s)'
  metrics:
    best_nse: 0.7487
    best_kge: 0.7905
    best_r: 0.9278
summary:
  total_tools: 6
  total_stage_docs: 6
  total_diagnostic_triplets: 21
  generated: '2026-08-15'
  generated_by: generate_ki_manifest.py (projection; regenerate any time)
