kiss_manifest_version: 1
model: SuperflexPy
binary_type: Python
verified: observed
install_dir: SuperflexPy

acquire:
  strategy: pip
  package: superflexpy
  produces: superflexpy

python_deps: [superflexpy]

reference_case: >-
  python -c "import superflexpy; print(superflexpy.__name__, 'importable')"

notes: >-
  OBSERVED 2026-08-18: `kiss init SuperflexPy` installed superflexpy 1.3.3 into a clean venv
  and the KI's own preflight_check.py passed with no hand-written recipe. The
  package name was resolved from the KI directory name and confirmed by import,
  not assumed from a PyPI name match.
