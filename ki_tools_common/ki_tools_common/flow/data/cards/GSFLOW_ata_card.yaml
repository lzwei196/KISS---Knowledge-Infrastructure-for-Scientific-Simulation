# ATA Card: GSFLOW
# Generated by Pipeline 1 on 2026-04-26

ata_card_version: '1.0'
generated: '2026-04-26'
generator: ata_pipeline1_card_gen.py
identity:
  model_id: GSFLOW
  model_name: GSFLOW
  domain: hydrology
  secondary_domains: []
  language: unknown
  binary_path: ''
inputs:
- canonical_id: air_temperature
  local_name: Temperature
  unit: °F (default) or °C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: precipitation_rate
  local_name: Precipitation
  unit: inches/day (default) or mm/day
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: air_temperature
  local_name: temp
  unit: Temperature distribution module
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: precipitation_rate
  local_name: precip
  unit: Precipitation distribution module
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: null
  local_name: Snow
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: forcing
- canonical_id: null
  local_name: Solar
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: forcing
- canonical_id: wind_speed
  local_name: Wind
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
outputs:
- canonical_id: air_temperature
  local_name: Temperature
  unit: °F (default) or °C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: echoed_input
  origin_confidence: high
- canonical_id: evapotranspiration
  local_name: ET
  unit: inches/day
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: surface_runoff
  local_name: Runoff
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: null
  local_name: Snow
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
- canonical_id: evapotranspiration
  local_name: Evapotranspiration
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: soil_moisture
  local_name: Soil_moisture
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: null
  local_name: Flood
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
spatial:
  type: regular_grid
  resolution: 0.25deg
  crs: EPSG:4326
  source: skill_md
temporal:
  resolution: daily
  calendar: standard
  source: format_spec
risks:
  known_traps:
  - canonical_var: air_temperature
    trap_id: dt_005
    description: 'Temperature lapse rate corrections are wildly wrong. Valley HRUs

      are too cold or mountain HRUs too warm. Snow line is at wrong

      elevation. Streamflow timing is incorrect.

      '
    detection: 'DEM elevation units (meters) don''t match GSFLOW elevation units

      (feet, via elev_units parameter). If DEM is in meters but GSFLOW

      expects feet, lapse rate corrections are applied to wrong values.

      1 met'
    fix: 'Either convert DEM to feet before extracting hru_elev, or set

      elev_units=1 (meters) in the parameter file. Verify by checking

      hru_elev values against known basin elevations.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: air_temperature
    trap_id: dt_017
    description: 'Simulated discharge is approximately 35× higher or lower than

      observed discharge. Temporal pattern may look correct but magnitude

      is completely wrong.

      '
    detection: 'Output unit mismatch. GSFLOW outputs basin_cfs (cubic feet per

      second) but observations are in m³/s. The conversion factor is

      0.028317. Comparing cfs to cms directly gives 35× error.

      '
    fix: 'Convert basin_cfs to m³/s: multiply by CFS_TO_CMS = 0.028317.

      Or convert basin_stflow (inches) to m³/s using basin area:

      Q_cms = stflow_in × area_km² × 25.4 × 1000 / 86400

      '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: surface_runoff
    trap_id: dt_011
    description: 'No streamflow generated. basin_cfs is always 0. All water goes to

      soil and groundwater storage but never reaches streams.

      '
    detection: 'Cascade flow network not defined or improperly configured.

      HRUs are not connected to stream segments, so surface runoff and

      interflow have no pathway to streams. Water accumulates in HRUs.

      '
    fix: 'Verify cascade parameters are defined:

      - ncascade > 0

      - hru_up_id and hru_down_id arrays properly assigned

      - Terminal HRUs cascade to stream segments (hru_segment)

      Alternatively, use strmflow_in_out m'
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: surface_runoff
    trap_id: dt_012
    description: 'Simulated peak flows are much higher than observed, while baseflow

      is too low. Hydrograph is too flashy with rapid rise and fall.

      '
    detection: 'Groundwater parameters (ssr2gw_rate, gwflow_coef) are too low,

      causing insufficient groundwater recharge and baseflow. Most water

      runs off as surface flow or quick interflow.

      '
    fix: 'Increase ssr2gw_rate (0.1-0.5) and gwflow_coef (0.01-0.05).

      Also check soil_moist_max — if too low, soil fills quickly and

      generates excess surface runoff.

      '
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: surface_runoff
    trap_id: dt_018
    description: 'Basin area in model is wrong. Water balance doesn''t close. Runoff

      volume is systematically over or underestimated by a constant factor.

      '
    detection: 'HRU area (hru_area) is in wrong units. PRMS expects acres.

      Common mistake: using km² or m². 1 km² = 247.105 acres.

      If hru_area is in km², volumes will be off by 247×.

      '
    fix: 'Convert basin area to acres: km² × 247.105 or m² / 4046.86.

      Verify: sum(hru_area) should equal basin area in acres.

      Bengbu basin: ~121,330 km² ≈ 29,977,000 acres.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: wind_speed
    trap_id: dt_013
    description: '"File not found" errors when running on Linux. Control file was

      created on Windows and contains backslash path separators.

      '
    detection: 'Windows-style paths (\..\input\prms\file.params) are not valid

      on Linux. The backslashes are interpreted as escape characters

      or literal characters, not path separators.

      '
    fix: 'Convert all backslashes to forward slashes in control file and

      MODFLOW name file: sed -i ''s|\\|/|g'' *.control *.nam

      The build_control_file.py tool always uses forward slashes.

      '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: wind_speed
    trap_id: dt_015
    description: 'GSFLOW crashes during first timestep with "data file read error"

      or "wrong number of values" in data file.

      '
    detection: 'The .day file has wrong number of values per line. GSFLOW expects

      exactly nhru values after the 6 date fields. Fewer values → crash.

      More values → silently ignored (but usually causes alignment issues'
    fix: 'Verify each data line has exactly 6 + nhru columns.

      Check nhru dimension in parameter file matches .day file column count.

      Watch for inconsistent line endings (Windows CRLF vs Unix LF).

      '
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: dt_001
    description: 'Model runs successfully but simulated streamflow is 25× higher than

      observed. Massive flooding in all seasons. Basin water balance shows

      precipitation far exceeding any reasonable value.

      '
    detection: 'Precipitation units not converted from mm/day (CMFD source) to

      inches/day (GSFLOW precip_units=0). The factor of 25.4 was missed.

      GSFLOW interprets mm values as inches, inflating precipitation by 25.4'
    fix: 'Multiply precipitation by MM_TO_INCHES = 1/25.4 = 0.03937 before

      writing to .day or .data file. Alternatively, set precip_units=1 (mm)

      in the parameter file to keep mm units. Verify: annual precipitat'
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_002
    description: 'No snow accumulation even in winter for basins that should have snow.

      ET is unrealistically high in winter. All precipitation falls as rain

      regardless of season.

      '
    detection: 'Temperature data in Celsius being read by GSFLOW as Fahrenheit

      (temp_units=0). 0°C is interpreted as 0°F, which is -17.8°C — but

      during summer, 25°C is read as 25°F (-3.9°C), which is still cold.

      Actu'
    fix: 'Convert temperature: K→°F = (K-273.15)×9/5+32 or K→°C = K-273.15.

      Match temp_units in parameter file to actual data units.

      Check: winter temperatures should be near/below 32°F (0°C) for

      snow-producing'
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_004
    description: 'All precipitation is stored in soil with almost no streamflow generated.

      Soil moisture never reaches capacity. The soil acts as an infinite sponge.

      '
    detection: 'Soil moisture capacity (soil_moist_max) computed in mm but GSFLOW

      expects inches. A typical soil_moist_max of 200mm becomes 200 inches

      (5080mm) — an impossibly large soil reservoir that never fills.

      '
    fix: 'Convert soil depth/capacity from mm to inches: divide by 25.4.

      Typical soil_moist_max range: 2-20 inches (50-500 mm).

      If soil_moist_max > 30 inches, likely still in mm.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_010
    description: 'GSFLOW aborts with "modflow_time_zero must precede simulation

      start_time" or similar timing error.

      '
    detection: 'The modflow_time_zero control parameter is set to the same date or

      a date after start_time. GSFLOW requires modflow_time_zero to be

      at least 1 day before start_time.

      '
    fix: 'Set modflow_time_zero to 1 day before start_time. For example,

      if start_time = 1980,10,1, set modflow_time_zero = 1980,9,30.

      '
    is_unit_trap: false
    automatable: false
    severity: medium
  - trap_id: dt_001
    triplet_ref: ''
    description: 'Model runs successfully but simulated streamflow is 25× higher than

      observed. Massive flooding in all seasons. Basin water balance shows

      precipitation far exceeding any reasonable value.

      '
    affected_output: 'Precipitation units not converted from mm/day (CMFD source) to

      inches/day (GSFLOW precip_units=0). T'
    detection: 'Precipitation units not converted from mm/day (CMFD source) to

      inches/day (GSFLOW precip_units=0). The factor of 25.4 was missed.

      GSFLOW interprets mm values as inches, inflating precipitation by 25.4'
    fix: 'Multiply precipitation by MM_TO_INCHES = 1/25.4 = 0.03937 before

      writing to .day or .data file. Alternatively, set precip_units=1 (mm)

      in the parameter file to keep mm units. Verify: annual precipitat'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_002
    triplet_ref: ''
    description: 'No snow accumulation even in winter for basins that should have snow.

      ET is unrealistically high in winter. All precipitation falls as rain

      regardless of season.

      '
    affected_output: 'Temperature data in Celsius being read by GSFLOW as Fahrenheit

      (temp_units=0). 0°C is interpreted as'
    detection: 'Temperature data in Celsius being read by GSFLOW as Fahrenheit

      (temp_units=0). 0°C is interpreted as 0°F, which is -17.8°C — but

      during summer, 25°C is read as 25°F (-3.9°C), which is still cold.

      Actu'
    fix: 'Convert temperature: K→°F = (K-273.15)×9/5+32 or K→°C = K-273.15.

      Match temp_units in parameter file to actual data units.

      Check: winter temperatures should be near/below 32°F (0°C) for

      snow-producing'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_003
    triplet_ref: ''
    description: 'Simulated ET is approximately half the expected value. Soil moisture

      remains too high. Streamflow is moderately overestimated due to

      insufficient ET extraction.

      '
    affected_output: 'Solar radiation not converted from W/m² (CMFD source) to

      Langleys/day (GSFLOW internal). The convers'
    detection: 'Solar radiation not converted from W/m² (CMFD source) to

      Langleys/day (GSFLOW internal). The conversion factor is ~2.065.

      Without conversion, radiation appears ~2× too low, reducing

      computed potential'
    fix: 'Convert: Langleys/day = W/m² × 86400/41868 ≈ W/m² × 2.065.

      Verify: peak summer solar radiation should be 400-700 Langleys/day

      (200-340 W/m²). If values are < 200, likely not converted.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_004
    triplet_ref: ''
    description: 'All precipitation is stored in soil with almost no streamflow generated.

      Soil moisture never reaches capacity. The soil acts as an infinite sponge.

      '
    affected_output: 'Soil moisture capacity (soil_moist_max) computed in mm but GSFLOW

      expects inches. A typical soil_moi'
    detection: 'Soil moisture capacity (soil_moist_max) computed in mm but GSFLOW

      expects inches. A typical soil_moist_max of 200mm becomes 200 inches

      (5080mm) — an impossibly large soil reservoir that never fills.

      '
    fix: 'Convert soil depth/capacity from mm to inches: divide by 25.4.

      Typical soil_moist_max range: 2-20 inches (50-500 mm).

      If soil_moist_max > 30 inches, likely still in mm.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_005
    triplet_ref: ''
    description: 'Temperature lapse rate corrections are wildly wrong. Valley HRUs

      are too cold or mountain HRUs too warm. Snow line is at wrong

      elevation. Streamflow timing is incorrect.

      '
    affected_output: 'DEM elevation units (meters) don''t match GSFLOW elevation units

      (feet, via elev_units parameter). If'
    detection: 'DEM elevation units (meters) don''t match GSFLOW elevation units

      (feet, via elev_units parameter). If DEM is in meters but GSFLOW

      expects feet, lapse rate corrections are applied to wrong values.

      1 met'
    fix: 'Either convert DEM to feet before extracting hru_elev, or set

      elev_units=1 (meters) in the parameter file. Verify by checking

      hru_elev values against known basin elevations.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_006
    triplet_ref: ''
    description: 'GSFLOW crashes immediately at startup with error about reading

      control file or "unexpected end of file" error.

      '
    affected_output: 'Missing #### delimiter between parameter blocks in control file.

      Each parameter block must start and'
    detection: 'Missing #### delimiter between parameter blocks in control file.

      Each parameter block must start and end with ####. A single missing

      delimiter causes the parser to misalign all subsequent parameters.

      '
    fix: 'Ensure every parameter block in the control file is bounded by

      #### lines. Use build_control_file.py tool which handles delimiters

      automatically. Validate by counting #### lines: should be

      2 × (number'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_007
    triplet_ref: ''
    description: 'GSFLOW crashes during initialization with "dimension mismatch" or

      "array bounds exceeded" error message.

      '
    affected_output: 'Parameter array length doesn''t match declared dimension. For example,

      if nhru=128 but soil_moist_max'
    detection: 'Parameter array length doesn''t match declared dimension. For example,

      if nhru=128 but soil_moist_max has only 100 values, GSFLOW crashes

      when trying to read value 101.

      '
    fix: 'Verify each parameter has exactly the declared number of values.

      Check dimension declarations in parameter files match across all

      parameter files (nhru, nsegment, nssr must be consistent).

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_008
    triplet_ref: ''
    description: 'Parameter values are all zero or all the same default value.

      Model runs but produces uniform output across all HRUs.

      '
    affected_output: 'Wrong data type code in parameter file. Using code 1 (integer) for

      a float parameter truncates decim'
    detection: 'Wrong data type code in parameter file. Using code 1 (integer) for

      a float parameter truncates decimal values to 0. Using code 4 (string)

      for a numeric parameter reads garbage.

      '
    fix: 'Data type codes: 1=integer, 2=real (float), 4=character (string).

      Verify data type codes match parameter type. Most physical parameters

      are type 2 (real). soil_type and cov_type are type 1 (integer).

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_009
    triplet_ref: ''
    description: '"FAILED TO MEET SOLVER CONVERGENCE CRITERIA" repeated in MODFLOW

      listing file. Model may produce output but water budget doesn''t close.

      '
    affected_output: 'MODFLOW groundwater solver (PCG, NWT) cannot converge within the

      specified iterations. Common causes'
    detection: 'MODFLOW groundwater solver (PCG, NWT) cannot converge within the

      specified iterations. Common causes: unrealistic hydraulic conductivity,

      initial heads far from solution, dry cells with rewetting prob'
    fix: '1. Switch from PCG to NWT solver (more robust for unconfined flow)

      2. Increase MXITER in solver settings (e.g., 500→1000)

      3. Relax convergence criteria (HCLOSE: 0.001→0.01)

      4. Check hydraulic conducti'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_010
    triplet_ref: ''
    description: 'GSFLOW aborts with "modflow_time_zero must precede simulation

      start_time" or similar timing error.

      '
    affected_output: 'The modflow_time_zero control parameter is set to the same date or

      a date after start_time. GSFLOW r'
    detection: 'The modflow_time_zero control parameter is set to the same date or

      a date after start_time. GSFLOW requires modflow_time_zero to be

      at least 1 day before start_time.

      '
    fix: 'Set modflow_time_zero to 1 day before start_time. For example,

      if start_time = 1980,10,1, set modflow_time_zero = 1980,9,30.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_011
    triplet_ref: ''
    description: 'No streamflow generated. basin_cfs is always 0. All water goes to

      soil and groundwater storage but never reaches streams.

      '
    affected_output: 'Cascade flow network not defined or improperly configured.

      HRUs are not connected to stream segments'
    detection: 'Cascade flow network not defined or improperly configured.

      HRUs are not connected to stream segments, so surface runoff and

      interflow have no pathway to streams. Water accumulates in HRUs.

      '
    fix: 'Verify cascade parameters are defined:

      - ncascade > 0

      - hru_up_id and hru_down_id arrays properly assigned

      - Terminal HRUs cascade to stream segments (hru_segment)

      Alternatively, use strmflow_in_out m'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_012
    triplet_ref: ''
    description: 'Simulated peak flows are much higher than observed, while baseflow

      is too low. Hydrograph is too flashy with rapid rise and fall.

      '
    affected_output: 'Groundwater parameters (ssr2gw_rate, gwflow_coef) are too low,

      causing insufficient groundwater rech'
    detection: 'Groundwater parameters (ssr2gw_rate, gwflow_coef) are too low,

      causing insufficient groundwater recharge and baseflow. Most water

      runs off as surface flow or quick interflow.

      '
    fix: 'Increase ssr2gw_rate (0.1-0.5) and gwflow_coef (0.01-0.05).

      Also check soil_moist_max — if too low, soil fills quickly and

      generates excess surface runoff.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_013
    triplet_ref: ''
    description: '"File not found" errors when running on Linux. Control file was

      created on Windows and contains backslash path separators.

      '
    affected_output: 'Windows-style paths (\..\input\prms\file.params) are not valid

      on Linux. The backslashes are interpr'
    detection: 'Windows-style paths (\..\input\prms\file.params) are not valid

      on Linux. The backslashes are interpreted as escape characters

      or literal characters, not path separators.

      '
    fix: 'Convert all backslashes to forward slashes in control file and

      MODFLOW name file: sed -i ''s|\\|/|g'' *.control *.nam

      The build_control_file.py tool always uses forward slashes.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_014
    triplet_ref: ''
    description: 'Output directory errors. GSFLOW reports cannot open output files.

      No output files are created.

      '
    affected_output: 'Output directories specified in control file don''t exist. GSFLOW

      does NOT create directories automat'
    detection: 'Output directories specified in control file don''t exist. GSFLOW

      does NOT create directories automatically. If the control file

      specifies output/prms/ but that directory doesn''t exist, writes fail.

      '
    fix: 'Create all output directories before running:

      mkdir -p output/prms output/modflow

      Check all paths in csv_output_file, model_output_file, etc.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_015
    triplet_ref: ''
    description: 'GSFLOW crashes during first timestep with "data file read error"

      or "wrong number of values" in data file.

      '
    affected_output: 'The .day file has wrong number of values per line. GSFLOW expects

      exactly nhru values after the 6 da'
    detection: 'The .day file has wrong number of values per line. GSFLOW expects

      exactly nhru values after the 6 date fields. Fewer values → crash.

      More values → silently ignored (but usually causes alignment issues'
    fix: 'Verify each data line has exactly 6 + nhru columns.

      Check nhru dimension in parameter file matches .day file column count.

      Watch for inconsistent line endings (Windows CRLF vs Unix LF).

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_016
    triplet_ref: ''
    description: 'Validation metrics (NSE, KGE) are much worse than expected despite

      reasonable-looking hydrographs. NSE is strongly negative.

      '
    affected_output: 'Spinup period included in validation statistics. The first 1-3 years

      of simulation have unrealistic '
    detection: 'Spinup period included in validation statistics. The first 1-3 years

      of simulation have unrealistic initial conditions (dry soil, wrong

      groundwater heads), which dramatically inflate error statistics.'
    fix: 'Discard first 1-3 years as spinup period. For Bengbu 1980-1990:

      start model at 1980, validate only 1981-1990 (or even 1982-1990).

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_017
    triplet_ref: ''
    description: 'Simulated discharge is approximately 35× higher or lower than

      observed discharge. Temporal pattern may look correct but magnitude

      is completely wrong.

      '
    affected_output: 'Output unit mismatch. GSFLOW outputs basin_cfs (cubic feet per

      second) but observations are in m³/s.'
    detection: 'Output unit mismatch. GSFLOW outputs basin_cfs (cubic feet per

      second) but observations are in m³/s. The conversion factor is

      0.028317. Comparing cfs to cms directly gives 35× error.

      '
    fix: 'Convert basin_cfs to m³/s: multiply by CFS_TO_CMS = 0.028317.

      Or convert basin_stflow (inches) to m³/s using basin area:

      Q_cms = stflow_in × area_km² × 25.4 × 1000 / 86400

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_018
    triplet_ref: ''
    description: 'Basin area in model is wrong. Water balance doesn''t close. Runoff

      volume is systematically over or underestimated by a constant factor.

      '
    affected_output: 'HRU area (hru_area) is in wrong units. PRMS expects acres.

      Common mistake: using km² or m². 1 km² = '
    detection: 'HRU area (hru_area) is in wrong units. PRMS expects acres.

      Common mistake: using km² or m². 1 km² = 247.105 acres.

      If hru_area is in km², volumes will be off by 247×.

      '
    fix: 'Convert basin area to acres: km² × 247.105 or m² / 4046.86.

      Verify: sum(hru_area) should equal basin area in acres.

      Bengbu basin: ~121,330 km² ≈ 29,977,000 acres.

      '
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  unit_conversions: []
coupling:
  chain_position: bidirectional
  native_formats:
  - unspecified
  exchange_format: unspecified
  coupling_role: earth_system
quality:
  has_format_spec: true
  has_skill_md: true
  has_triplets: true
  n_inputs: 7
  n_outputs: 7
  n_inputs_canonical: 5
  n_outputs_canonical: 5
  n_unmapped: 4
  n_traps: 29
  n_conversions: 0
  spatial_complete: true
  temporal_complete: true
  unmapped_variables:
  - name: Snow
    direction: input
    reason: unmapped
  - name: Solar
    direction: input
    reason: unmapped
  - name: Snow
    direction: output
    reason: unmapped
  - name: Flood
    direction: output
    reason: unmapped
  validation_issues: []
  card_status: complete
