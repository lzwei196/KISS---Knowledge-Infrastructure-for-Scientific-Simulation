# ATA Card: PyAEZ
# Generated by Pipeline 1 on 2026-04-26

ata_card_version: '1.0'
generated: '2026-04-26'
generator: ata_pipeline1_card_gen.py
identity:
  model_id: PyAEZ
  model_name: PyAEZ v2.2 Knowledge Infrastructure
  domain: crop
  secondary_domains: []
  language: unknown
  binary_path: ''
inputs:
- canonical_id: precipitation_rate
  local_name: Precipitation
  unit: '`precipitation`'
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: air_temperature
  local_name: Temperature
  unit: °C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: null
  local_name: Radiation
  unit: W/m²
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: forcing
- canonical_id: precipitation_rate
  local_name: Rain
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
outputs:
- canonical_id: leaf_area_index
  local_name: LAI
  unit: unitless
  valid_range: null
  format: unspecified
  semantic_type: fraction
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: potential_evapotranspiration
  local_name: pet
  unit: mm/day
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: grain_yield
  local_name: yield
  unit: kg/ha
  valid_range: null
  format: unspecified
  semantic_type: rate
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: air_temperature
  local_name: Temperature
  unit: °C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: echoed_input
  origin_confidence: high
spatial:
  type: network
  resolution: 5deg
  crs: EPSG:4326
  source: skill_md
temporal:
  resolution: daily
  calendar: standard
  source: skill_md
risks:
  known_traps:
  - canonical_var: air_temperature
    trap_id: dt_001
    description: All yields are NaN or unrealistically high (>1e6 kg/ha). ETo values are extreme (>100 mm/day).
    detection: Temperature data is in Kelvin instead of Celsius. CMFD and MSWX store temperature in K. PyAEZ interprets 290
      K as 290°C, causing exponential vapor pressure overflow.
    fix: 'Convert temperature before input: temp_C = temp_K - 273.15. Verify: mean temperature should be -30 to +45°C, not
      240–320.'
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: grain_yield
    trap_id: dt_011
    description: KeyError or IndexError when reading crop parameters. Module II crashes during readCropandCropCycleParameters().
    detection: Crop parameter Excel file is missing required columns or the crop_name does not match a sheet name in the Excel
      file.
    fix: 'Verify Excel has all required columns: LAI, HI, legume, adaptability, min_cycle_length, max_cycle_length, kc, yloss_f,
      est_yield, D1, D2, pc. Check sheet name matches crop_name argument.'
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: grain_yield
    trap_id: dt_016
    description: All pixels have identical yield regardless of climate. Yield map is spatially uniform.
    detection: LGP indicators from Module I were not imported before simulation. Without lgp/lgpt5/lgpt10, the crop simulation
      uses fallback values that produce uniform results.
    fix: Call sim.ImportLGPandLGPT(lgp=lgp, lgpt5=lgpt5, lgpt10=lgpt10) BEFORE sim.simulateCropCycle(). These must come from
      Module I output.
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: dt_003
    description: LGP = 365 everywhere. All areas classified as per-humid. P/PET >> 1 across entire domain.
    detection: Precipitation is in mm/month instead of mm/day. Monthly totals fed as daily rates make rainfall appear 28–31×
      too high.
    fix: 'Divide monthly totals by days per month: precip_daily[m] = precip_monthly[m] / days_in_month[m]. Verify: typical
      daily precip is 0–50 mm/day.'
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_007
    description: Negative precipitation on some days. Negative ETo for a few days. Negative radiation values.
    detection: Cubic spline interpolation from 12 monthly to 365 daily values can produce negative overshoots, especially
      for variables with sharp seasonal transitions.
    fix: 'After interpolation, clamp values: precip = np.maximum(precip, 0). Or use daily input data directly (daily=True
      mode).'
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: dt_008
    description: Water stress factor (fc2) is too high or too low across all pixels uniformly. Yields are insensitive to precipitation
      patterns.
    detection: Default soil water parameters Sa=100 mm/m and D=1.0 m are used instead of site-specific values. Sa×D = available
      water = 100 mm, which may not match actual soil.
    fix: 'Provide spatially varying Sa from HWSD: sim.setSoilWaterParameters(Sa=sa_map, pc=0.5). Typical Sa: sand=60, loam=140,
      clay=120 mm/m.'
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_013
    description: fc4 = 0 for most pixels. Soil-constrained yield drops to zero across large areas.
    detection: Soil mapping unit (SMU) integer codes in the raster do not match the codes in the soil characteristics Excel.
      Common when HWSD versions differ (v1 vs v2) or when raster was reclassified.
    fix: 'Cross-check: unique values in soil_map raster must all appear in the SMU column of the Excel files. Re-derive soil
      Excel from the same HWSD version as the raster.'
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_014
    description: fc5 is extremely low even for gentle terrain. Terrain reduction factor penalizes flat areas.
    detection: Slope is in degrees instead of percent. 5° ≈ 8.7% slope, but PyAEZ reads 5 as 5% which may map to a different
      reduction class than intended.
    fix: 'Convert: slope_pct = np.tan(np.radians(slope_deg)) * 100. Or verify slope GeoTIFF metadata for units.'
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_015
    description: Economic analysis gives revenue values that seem 1000× too large or too small.
    detection: Yield units mismatch between Module V output (kg/ha) and Module VI input expectation (ton/ha). Module V outputs
      kg/ha but the economic function expects ton/ha.
    fix: 'Divide by 1000 before Module VI: yield_tons = terrain_yield_rain / 1000. Check that cost and price units are consistent
      (per-ton or per-kg).'
    is_unit_trap: true
    automatable: false
    severity: high
  - trap_id: dt_001
    triplet_ref: ''
    description: All yields are NaN or unrealistically high (>1e6 kg/ha). ETo values are extreme (>100 mm/day).
    affected_output: Temperature data is in Kelvin instead of Celsius. CMFD and MSWX store temperature in K. PyAEZ interp
    detection: Temperature data is in Kelvin instead of Celsius. CMFD and MSWX store temperature in K. PyAEZ interprets 290
      K as 290°C, causing exponential vapor pressure overflow.
    fix: 'Convert temperature before input: temp_C = temp_K - 273.15. Verify: mean temperature should be -30 to +45°C, not
      240–320.'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_002
    triplet_ref: ''
    description: LGP = 0 for all pixels. ETo is extremely large (>50 mm/day). P/PET ratio near zero everywhere.
    affected_output: 'Relative humidity is in percentage (0–100) instead of fraction (0–1). CMFD rhum variable is 0–100%. '
    detection: Relative humidity is in percentage (0–100) instead of fraction (0–1). CMFD rhum variable is 0–100%. PyAEZ uses
      RH directly in Penman-Monteith; values >1 produce extreme saturation deficit.
    fix: 'Divide by 100: rh_frac = rh_pct / 100.0. Verify: max value should be ≤1.0.'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_003
    triplet_ref: ''
    description: LGP = 365 everywhere. All areas classified as per-humid. P/PET >> 1 across entire domain.
    affected_output: Precipitation is in mm/month instead of mm/day. Monthly totals fed as daily rates make rainfall appe
    detection: Precipitation is in mm/month instead of mm/day. Monthly totals fed as daily rates make rainfall appear 28–31×
      too high.
    fix: 'Divide monthly totals by days per month: precip_daily[m] = precip_monthly[m] / days_in_month[m]. Verify: typical
      daily precip is 0–50 mm/day.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_004
    triplet_ref: ''
    description: Results vary wildly across pixels with no spatial coherence. Patterns look like noise despite correct mean
      values.
    affected_output: Climate 3D arrays have wrong axis order. Source data is (time, lat, lon) but PyAEZ expects (lat, lon
    detection: Climate 3D arrays have wrong axis order. Source data is (time, lat, lon) but PyAEZ expects (lat, lon, time).
      Each pixel reads the wrong time series.
    fix: 'Transpose arrays: data = np.transpose(data, (1, 2, 0)). Verify shape: last dimension should be 12 (monthly) or 365
      (daily).'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_005
    triplet_ref: ''
    description: ETo has a strong north-south gradient that is inverted. Solar radiation calculations produce wrong day lengths.
    affected_output: lat_min and lat_max are swapped or set to wrong values. These define the latitude grid used for sola
    detection: lat_min and lat_max are swapped or set to wrong values. These define the latitude grid used for solar declination
      and day length calculations.
    fix: Ensure lat_min < lat_max and both match the actual spatial extent of your input data. For Southern hemisphere, use
      negative values.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_006
    triplet_ref: ''
    description: ETo is ~20% higher than expected. Water stress (fc2) is slightly too severe. LGP is slightly too short.
    affected_output: Wind speed is at 10m reference height instead of 2m. CMFD and many reanalysis products provide 10m w
    detection: Wind speed is at 10m reference height instead of 2m. CMFD and many reanalysis products provide 10m wind. PyAEZ
      Penman-Monteith assumes 2m.
    fix: 'Apply height correction: u2 = u10 * 4.87 / ln(67.8 * 10 - 5.42). The correction factor is approximately 0.748.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_007
    triplet_ref: ''
    description: Negative precipitation on some days. Negative ETo for a few days. Negative radiation values.
    affected_output: Cubic spline interpolation from 12 monthly to 365 daily values can produce negative overshoots, espe
    detection: Cubic spline interpolation from 12 monthly to 365 daily values can produce negative overshoots, especially
      for variables with sharp seasonal transitions.
    fix: 'After interpolation, clamp values: precip = np.maximum(precip, 0). Or use daily input data directly (daily=True
      mode).'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_008
    triplet_ref: ''
    description: Water stress factor (fc2) is too high or too low across all pixels uniformly. Yields are insensitive to precipitation
      patterns.
    affected_output: Default soil water parameters Sa=100 mm/m and D=1.0 m are used instead of site-specific values. Sa×D
    detection: Default soil water parameters Sa=100 mm/m and D=1.0 m are used instead of site-specific values. Sa×D = available
      water = 100 mm, which may not match actual soil.
    fix: 'Provide spatially varying Sa from HWSD: sim.setSoilWaterParameters(Sa=sa_map, pc=0.5). Typical Sa: sand=60, loam=140,
      clay=120 mm/m.'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_009
    triplet_ref: ''
    description: Module II takes hours for a 100×100 grid. CPU usage is 100% on single core. No Numba compilation messages
      at startup.
    affected_output: Numba JIT compiler is not installed or not functioning. PyAEZ's core loops (ETOCalc, BioMassCalc, Cr
    detection: Numba JIT compiler is not installed or not functioning. PyAEZ's core loops (ETOCalc, BioMassCalc, CropWatCalc)
      fall back to pure Python, which is 10–100× slower.
    fix: 'Install Numba: pip install numba>=0.56 llvmlite>=0.40.1. Verify: python -c ''import numba; print(numba.__version__)''.
      First run will be slow due to JIT compilation.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_010
    triplet_ref: ''
    description: All outputs are zero or empty. No pixels are processed. Module runs instantly without computation.
    affected_output: 'Study area mask is set incorrectly. If mask_value matches all pixels in the mask array, or mask and '
    detection: Study area mask is set incorrectly. If mask_value matches all pixels in the mask array, or mask and data arrays
      have different shapes, no pixels are processed.
    fix: 'Check: np.sum(mask != mask_value) should be >0. Verify mask.shape matches climate array shape[:2]. mask_value=0
      means 0-valued pixels are excluded.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_011
    triplet_ref: ''
    description: KeyError or IndexError when reading crop parameters. Module II crashes during readCropandCropCycleParameters().
    affected_output: Crop parameter Excel file is missing required columns or the crop_name does not match a sheet name i
    detection: Crop parameter Excel file is missing required columns or the crop_name does not match a sheet name in the Excel
      file.
    fix: 'Verify Excel has all required columns: LAI, HI, legume, adaptability, min_cycle_length, max_cycle_length, kc, yloss_f,
      est_yield, D1, D2, pc. Check sheet name matches crop_name argument.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_012
    triplet_ref: ''
    description: Biomass calculation produces very low values. Yield is ~10× lower than expected. Ac/Bc ratio is off.
    affected_output: Solar radiation is in MJ/m²/day instead of W/m². PyAEZ expects W/m² and internally converts to cal/c
    detection: Solar radiation is in MJ/m²/day instead of W/m². PyAEZ expects W/m² and internally converts to cal/cm²/day.
      MJ/m²/day values (0–30) are ~11.6× too low for W/m² input.
    fix: 'Convert: srad_wm2 = srad_mjm2day * 1e6 / 86400 (or multiply by 11.574). Verify: typical range 50–350 W/m².'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_013
    triplet_ref: ''
    description: fc4 = 0 for most pixels. Soil-constrained yield drops to zero across large areas.
    affected_output: Soil mapping unit (SMU) integer codes in the raster do not match the codes in the soil characteristi
    detection: Soil mapping unit (SMU) integer codes in the raster do not match the codes in the soil characteristics Excel.
      Common when HWSD versions differ (v1 vs v2) or when raster was reclassified.
    fix: 'Cross-check: unique values in soil_map raster must all appear in the SMU column of the Excel files. Re-derive soil
      Excel from the same HWSD version as the raster.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_014
    triplet_ref: ''
    description: fc5 is extremely low even for gentle terrain. Terrain reduction factor penalizes flat areas.
    affected_output: 'Slope is in degrees instead of percent. 5° ≈ 8.7% slope, but PyAEZ reads 5 as 5% which may map to a '
    detection: Slope is in degrees instead of percent. 5° ≈ 8.7% slope, but PyAEZ reads 5 as 5% which may map to a different
      reduction class than intended.
    fix: 'Convert: slope_pct = np.tan(np.radians(slope_deg)) * 100. Or verify slope GeoTIFF metadata for units.'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_015
    triplet_ref: ''
    description: Economic analysis gives revenue values that seem 1000× too large or too small.
    affected_output: Yield units mismatch between Module V output (kg/ha) and Module VI input expectation (ton/ha). Modul
    detection: Yield units mismatch between Module V output (kg/ha) and Module VI input expectation (ton/ha). Module V outputs
      kg/ha but the economic function expects ton/ha.
    fix: 'Divide by 1000 before Module VI: yield_tons = terrain_yield_rain / 1000. Check that cost and price units are consistent
      (per-ton or per-kg).'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_016
    triplet_ref: ''
    description: All pixels have identical yield regardless of climate. Yield map is spatially uniform.
    affected_output: LGP indicators from Module I were not imported before simulation. Without lgp/lgpt5/lgpt10, the crop
    detection: LGP indicators from Module I were not imported before simulation. Without lgp/lgpt5/lgpt10, the crop simulation
      uses fallback values that produce uniform results.
    fix: Call sim.ImportLGPandLGPT(lgp=lgp, lgpt5=lgpt5, lgpt10=lgpt10) BEFORE sim.simulateCropCycle(). These must come from
      Module I output.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_017
    triplet_ref: ''
    description: LGP values are implausibly high in arid regions. Water balance never depletes.
    affected_output: Soil available moisture (Sa) or rooting depth (D) set too high, giving large water storage buffer. S
    detection: Soil available moisture (Sa) or rooting depth (D) set too high, giving large water storage buffer. Sa=100 mm/m
      with D=2m = 200mm buffer, which can sustain crops through dry spells.
    fix: 'Use realistic values: Sa from HWSD (typically 50–150 mm/m), D appropriate for the crop (0.5–1.5 m). For initial
      screening, Sa=100, D=1.0 is standard.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  unit_conversions: []
coupling:
  chain_position: bidirectional
  native_formats:
  - unspecified
  exchange_format: unspecified
  coupling_role: earth_system
quality:
  has_format_spec: true
  has_skill_md: true
  has_triplets: true
  n_inputs: 4
  n_outputs: 4
  n_inputs_canonical: 3
  n_outputs_canonical: 4
  n_unmapped: 1
  n_traps: 26
  n_conversions: 0
  spatial_complete: true
  temporal_complete: true
  unmapped_variables:
  - name: Radiation
    direction: input
    reason: unmapped
  validation_issues: []
  card_status: complete
