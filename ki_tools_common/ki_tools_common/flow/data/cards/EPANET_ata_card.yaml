# ATA Card: EPANET
# Generated by Pipeline 1 on 2026-04-26

ata_card_version: '1.0'
generated: '2026-04-26'
generator: ata_pipeline1_card_gen.py
identity:
  model_id: EPANET
  model_name: EPANET 2.2
  domain: unknown
  secondary_domains: []
  language: unknown
  binary_path: ''
inputs:
- canonical_id: air_pressure
  local_name: Pressure
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
outputs:
- canonical_id: surface_runoff
  local_name: runoff
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: hydraulic_head
  local_name: Hydraulic_Head
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: concentration
  local_name: Concentration
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: concentration
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: air_pressure
  local_name: pressure
  unit: m
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: echoed_input
  origin_confidence: high
- canonical_id: discharge
  local_name: flow_rate
  unit: m3/s
  valid_range: null
  format: unspecified
  semantic_type: volumetric_flow
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: null
  local_name: water_age
  unit: hours
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
spatial:
  type: point
  resolution: unspecified
  crs: EPSG:4326
  source: skill_md
temporal:
  resolution: hourly
  calendar: standard
  source: skill_md
risks:
  known_traps:
  - canonical_var: air_pressure
    trap_id: T02
    description: Negative pressures at multiple junctions in DDA mode
    detection: Demand-Driven Analysis (DDA) forces full demand delivery regardless of available pressure. When supply capacity
      is insufficient for the specified demands, the solver produces physically impossible neg
    fix: "1. Switch to Pressure Driven Analysis (PDA) in [OPTIONS]:\n   DEMAND MODEL PDA\n   MINIMUM PRESSURE 0\n   REQUIRED\
      \ PRESSURE 25\n2. Or reduce demands to match system capacity. 3. Or increase supply (raise "
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: air_pressure
    trap_id: T04
    description: Pressures are 3.28× too high or too low across the entire network
    detection: Elevation values were entered in the wrong length unit — feet instead of meters or vice versa. Since head =
      elevation + pressure/γ, a systematic elevation error shifts all pressures by the same factor
    fix: "1. Check flow units: LPS/LPM/MLD/CMH/CMD → elevations must be in meters.\n   CFS/GPM/MGD/IMGD/AFD → elevations must\
      \ be in feet.\n2. Convert: elevation_m = elevation_ft × 0.3048 (or ÷ 3.28084). 3. Update"
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: hydraulic_head
    trap_id: T01
    description: 'Error 110: Cannot solve network hydraulic equations'
    detection: 'The network is either disconnected (isolated subnetworks), has no fixed-grade node (reservoir or tank), or
      has zero-diameter pipes creating singularities in the hydraulic matrix.

      '
    fix: "1. Check network connectivity — every node must be reachable from at\n   least one reservoir or tank.\n2. Ensure\
      \ at least one reservoir or tank with known head exists. 3. Search for zero-diameter pipes "
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: hydraulic_head
    trap_id: T05
    description: Hazen-Williams headloss is unrealistically high or near zero
    detection: Roughness coefficient was entered as Darcy-Weisbach roughness height (e.g., 0.001-0.1 mm) instead of Hazen-Williams
      C-factor (e.g., 100-150), or vice versa. These are completely different scales and m
    fix: "1. Verify [OPTIONS] Headloss matches roughness values:\n   H-W: C-factor 50-160 (higher = smoother). Typical: new\
      \ PVC=150, old CI=100.\n   D-W: roughness height in mm (SI) or 10^-3 ft (US). Typical: 0.0"
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: discharge
    trap_id: T03
    description: Pipe diameters appear unrealistically small — all pipes < 1 inch
    detection: 'Unit mismatch: pipe diameters were entered in millimeters but the model uses US Customary units (expects inches).
      A 300 mm pipe becomes 300 inches if not converted, or a 12 mm pipe when 12 inches was '
    fix: '1. Verify flow units in [OPTIONS] — CFS/GPM/MGD/IMGD/AFD require inches. 2. Convert: diameter_inches = diameter_mm
      / 25.4 3. Use convert_network_params.py --source-units SI --target-units US

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: discharge
    trap_id: T06
    description: Tank overflows or empties immediately at simulation start
    detection: 'Tank initial level is set outside the min/max level range, or tank diameter is in the wrong units (e.g., 70
      mm instead of 70 ft), making the tank volume effectively zero.

      '
    fix: "1. Verify: min_level < init_level < max_level. 2. Tank diameter uses feet (US) or meters (SI) — NOT inches or mm.\
      \ 3. Check that bottom elevation + max_level gives a reasonable water\n   surface elevati"
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: discharge
    trap_id: T07
    description: Pump cannot deliver flow — status shows XHEAD or XFLOW
    detection: XHEAD means the pump cannot provide enough head to overcome the system head requirement. XFLOW means flow exceeds
      the pump's maximum rated flow. The pump curve may be incorrectly specified or the syst
    fix: "1. Check pump curve: HEAD curve should show decreasing head with\n   increasing flow. At least one point is required.\n\
      2. Verify pump curve units match flow units in [OPTIONS]. 3. If using POWER keyword"
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: discharge
    trap_id: T08
    description: Water quality (chlorine) remains at zero throughout simulation
    detection: 'Either quality analysis is not enabled, initial quality is zero everywhere, or no quality source is defined
      to inject chemical into the network.

      '
    fix: '1. Enable quality: [OPTIONS] Quality Chlorine mg/L 2. Set initial quality: [QUALITY] section with values > 0 at
      source nodes. 3. Define source: [SOURCES] section with CONCEN, SETPOINT, or FLOWPACED ty'
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: discharge
    trap_id: T14
    description: CONCEN source on internal junction has no visible effect on downstream quality
    detection: 'CONCEN source type only sets the concentration of external inflow entering a node. If the node has no external
      inflow (demand = 0 or demand is withdrawal), the CONCEN source has no effect.

      '
    fix: "1. Use SETPOINT source type to force a concentration at the node:\n   [SOURCES] NodeID SETPOINT 2.0\n2. Or use FLOWPACED\
      \ to add to the passing concentration:\n   [SOURCES] NodeID FLOWPACED 0.5\n3. CONCEN "
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: discharge
    trap_id: T16
    description: Demand in LPS but model expects GPM — demands are 15.85× too large
    detection: 'Flow unit mismatch between source data (SI, liters per second) and EPANET model (US, gallons per minute).
      1 LPS = 15.8503 GPM.

      '
    fix: '1. Convert demands: demand_GPM = demand_LPS × 15.8503 2. Or change [OPTIONS] Units to LPS (but then ALL units must
      be SI). 3. Use convert_demands_to_inp.py with --input-units SI --units GPM.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - trap_id: T01
    triplet_ref: ''
    description: 'Error 110: Cannot solve network hydraulic equations'
    affected_output: The network is either disconnected (isolated subnetworks), has no fixed-grade node (reservoir or tan
    detection: 'The network is either disconnected (isolated subnetworks), has no fixed-grade node (reservoir or tank), or
      has zero-diameter pipes creating singularities in the hydraulic matrix.

      '
    fix: "1. Check network connectivity — every node must be reachable from at\n   least one reservoir or tank.\n2. Ensure\
      \ at least one reservoir or tank with known head exists. 3. Search for zero-diameter pipes "
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T02
    triplet_ref: ''
    description: Negative pressures at multiple junctions in DDA mode
    affected_output: Demand-Driven Analysis (DDA) forces full demand delivery regardless of available pressure. When supp
    detection: Demand-Driven Analysis (DDA) forces full demand delivery regardless of available pressure. When supply capacity
      is insufficient for the specified demands, the solver produces physically impossible neg
    fix: "1. Switch to Pressure Driven Analysis (PDA) in [OPTIONS]:\n   DEMAND MODEL PDA\n   MINIMUM PRESSURE 0\n   REQUIRED\
      \ PRESSURE 25\n2. Or reduce demands to match system capacity. 3. Or increase supply (raise "
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T03
    triplet_ref: ''
    description: Pipe diameters appear unrealistically small — all pipes < 1 inch
    affected_output: 'Unit mismatch: pipe diameters were entered in millimeters but the model uses US Customary units (exp'
    detection: 'Unit mismatch: pipe diameters were entered in millimeters but the model uses US Customary units (expects inches).
      A 300 mm pipe becomes 300 inches if not converted, or a 12 mm pipe when 12 inches was '
    fix: '1. Verify flow units in [OPTIONS] — CFS/GPM/MGD/IMGD/AFD require inches. 2. Convert: diameter_inches = diameter_mm
      / 25.4 3. Use convert_network_params.py --source-units SI --target-units US

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T04
    triplet_ref: ''
    description: Pressures are 3.28× too high or too low across the entire network
    affected_output: Elevation values were entered in the wrong length unit — feet instead of meters or vice versa. Since
    detection: Elevation values were entered in the wrong length unit — feet instead of meters or vice versa. Since head =
      elevation + pressure/γ, a systematic elevation error shifts all pressures by the same factor
    fix: "1. Check flow units: LPS/LPM/MLD/CMH/CMD → elevations must be in meters.\n   CFS/GPM/MGD/IMGD/AFD → elevations must\
      \ be in feet.\n2. Convert: elevation_m = elevation_ft × 0.3048 (or ÷ 3.28084). 3. Update"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T05
    triplet_ref: ''
    description: Hazen-Williams headloss is unrealistically high or near zero
    affected_output: Roughness coefficient was entered as Darcy-Weisbach roughness height (e.g., 0.001-0.1 mm) instead of
    detection: Roughness coefficient was entered as Darcy-Weisbach roughness height (e.g., 0.001-0.1 mm) instead of Hazen-Williams
      C-factor (e.g., 100-150), or vice versa. These are completely different scales and m
    fix: "1. Verify [OPTIONS] Headloss matches roughness values:\n   H-W: C-factor 50-160 (higher = smoother). Typical: new\
      \ PVC=150, old CI=100.\n   D-W: roughness height in mm (SI) or 10^-3 ft (US). Typical: 0.0"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T06
    triplet_ref: ''
    description: Tank overflows or empties immediately at simulation start
    affected_output: Tank initial level is set outside the min/max level range, or tank diameter is in the wrong units (e
    detection: 'Tank initial level is set outside the min/max level range, or tank diameter is in the wrong units (e.g., 70
      mm instead of 70 ft), making the tank volume effectively zero.

      '
    fix: "1. Verify: min_level < init_level < max_level. 2. Tank diameter uses feet (US) or meters (SI) — NOT inches or mm.\
      \ 3. Check that bottom elevation + max_level gives a reasonable water\n   surface elevati"
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: T07
    triplet_ref: ''
    description: Pump cannot deliver flow — status shows XHEAD or XFLOW
    affected_output: XHEAD means the pump cannot provide enough head to overcome the system head requirement. XFLOW means
    detection: XHEAD means the pump cannot provide enough head to overcome the system head requirement. XFLOW means flow exceeds
      the pump's maximum rated flow. The pump curve may be incorrectly specified or the syst
    fix: "1. Check pump curve: HEAD curve should show decreasing head with\n   increasing flow. At least one point is required.\n\
      2. Verify pump curve units match flow units in [OPTIONS]. 3. If using POWER keyword"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T08
    triplet_ref: ''
    description: Water quality (chlorine) remains at zero throughout simulation
    affected_output: 'Either quality analysis is not enabled, initial quality is zero everywhere, or no quality source is '
    detection: 'Either quality analysis is not enabled, initial quality is zero everywhere, or no quality source is defined
      to inject chemical into the network.

      '
    fix: '1. Enable quality: [OPTIONS] Quality Chlorine mg/L 2. Set initial quality: [QUALITY] section with values > 0 at
      source nodes. 3. Define source: [SOURCES] section with CONCEN, SETPOINT, or FLOWPACED ty'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T09
    triplet_ref: ''
    description: Chlorine grows instead of decaying during simulation
    affected_output: Bulk reaction coefficient has the wrong sign. In EPANET, negative coefficients produce decay and pos
    detection: 'Bulk reaction coefficient has the wrong sign. In EPANET, negative coefficients produce decay and positive
      coefficients produce growth.

      '
    fix: "1. For chlorine decay, use NEGATIVE bulk coefficient:\n   [REACTIONS] Global Bulk -0.5  (decays at 0.5/day)\n2.\
      \ Positive values are only for growth reactions (e.g., DBP formation). 3. Check wall reactio"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T10
    triplet_ref: ''
    description: Simulation runs but report file shows no node/link results
    affected_output: 'The [REPORT] section does not specify which nodes and links to include. By default, EPANET does not '
    detection: 'The [REPORT] section does not specify which nodes and links to include. By default, EPANET does not report
      any results unless explicitly told to.

      '
    fix: "1. Add to [REPORT] section:\n   Nodes All\n   Links All\n2. Or specify individual node/link IDs:\n   Nodes J1 J2\
      \ J3\n   Links P1 P2 P3\n3. Ensure REPORT Timestep is set in [TIMES] section.\n"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T11
    triplet_ref: ''
    description: Pattern demand stays constant — pattern multipliers have no effect
    affected_output: Pattern timestep mismatch. The pattern may have 4 multipliers for a 24-hour cycle, but Pattern Times
    detection: Pattern timestep mismatch. The pattern may have 4 multipliers for a 24-hour cycle, but Pattern Timestep is
      set to 1 hour (expecting 24 multipliers), so only the first multiplier is used. Or the patter
    fix: "1. Verify Pattern Timestep in [TIMES] = Duration / number_of_multipliers.\n   4 multipliers for 24h → Pattern Timestep\
      \ 6:00\n   24 multipliers for 24h → Pattern Timestep 1:00\n2. Verify pattern IDs match"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T12
    triplet_ref: ''
    description: Wall reaction rate is 3.28× too fast or slow
    affected_output: 'First-order wall reaction coefficient units depend on the unit system: ft/day for US Customary, m/da'
    detection: 'First-order wall reaction coefficient units depend on the unit system: ft/day for US Customary, m/day for
      SI Metric. Using the wrong unit causes wall decay to be systematically off by the ft/m convers'
    fix: '1. US units (CFS/GPM/MGD): wall coefficient in ft/day. 2. SI units (LPS/LPM/MLD): wall coefficient in m/day. 3.
      Convert: coeff_m_per_day = coeff_ft_per_day × 0.3048.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T13
    triplet_ref: ''
    description: Binary output file (.out) cannot be parsed — struct.error or garbled values
    affected_output: The binary output file uses a specific format with 4-byte aligned records. Common parsing errors inc
    detection: 'The binary output file uses a specific format with 4-byte aligned records. Common parsing errors include:
      wrong byte offset (skipping prolog incorrectly), wrong endianness, or reading from a truncated'
    fix: 1. Verify file starts with magic number 516114521 (4-byte int). 2. Verify file ends with magic number 516114521 (last
      4 bytes). 3. Use the parse_epanet_output.py tool which handles the format correctl
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T14
    triplet_ref: ''
    description: CONCEN source on internal junction has no visible effect on downstream quality
    affected_output: CONCEN source type only sets the concentration of external inflow entering a node. If the node has n
    detection: 'CONCEN source type only sets the concentration of external inflow entering a node. If the node has no external
      inflow (demand = 0 or demand is withdrawal), the CONCEN source has no effect.

      '
    fix: "1. Use SETPOINT source type to force a concentration at the node:\n   [SOURCES] NodeID SETPOINT 2.0\n2. Or use FLOWPACED\
      \ to add to the passing concentration:\n   [SOURCES] NodeID FLOWPACED 0.5\n3. CONCEN "
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T15
    triplet_ref: ''
    description: Convergence oscillation — pump toggles open/closed every timestep
    affected_output: Simple controls have no hysteresis, causing the pump to turn on when tank drops below level X, which
    detection: Simple controls have no hysteresis, causing the pump to turn on when tank drops below level X, which raises
      the level above X, which turns the pump off, which drops the level below X again — oscillati
    fix: "1. Add hysteresis to controls:\n   LINK PUMP1 OPEN  IF NODE TANK1 BELOW 5\n   LINK PUMP1 CLOSED IF NODE TANK1 ABOVE\
      \ 20\n   (NOT: BELOW 5 and ABOVE 6 — too close)\n2. Or use [RULES] with combined condition"
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T16
    triplet_ref: ''
    description: Demand in LPS but model expects GPM — demands are 15.85× too large
    affected_output: Flow unit mismatch between source data (SI, liters per second) and EPANET model (US, gallons per min
    detection: 'Flow unit mismatch between source data (SI, liters per second) and EPANET model (US, gallons per minute).
      1 LPS = 15.8503 GPM.

      '
    fix: '1. Convert demands: demand_GPM = demand_LPS × 15.8503 2. Or change [OPTIONS] Units to LPS (but then ALL units must
      be SI). 3. Use convert_demands_to_inp.py with --input-units SI --units GPM.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T17
    triplet_ref: ''
    description: 'shared libraries error: libepanet2.so not found'
    affected_output: The runepanet executable dynamically links against libepanet2.so. The shared library is in the build
    detection: 'The runepanet executable dynamically links against libepanet2.so. The shared library is in the build/src/solver/
      directory but the system linker doesn''t know where to find it.

      '
    fix: "1. Set LD_LIBRARY_PATH:\n   export LD_LIBRARY_PATH=/path/to/build/src/solver:$LD_LIBRARY_PATH\n2. Or copy libepanet2.so\
      \ to a system library path (/usr/local/lib). 3. Or rebuild with -DBUILD_SHARED_LIBS="
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: T18
    triplet_ref: ''
    description: Water age exceeds simulation duration at some nodes
    affected_output: This is physically possible and not a bug. Water age is cumulative and represents the time since wat
    detection: 'This is physically possible and not a bug. Water age is cumulative and represents the time since water entered
      the network from a source. Dead-end nodes or nodes far from sources can accumulate water '
    fix: 1. This is expected behavior. Water age starts from 0 at reservoirs. 2. For existing systems, set initial water age
      in [QUALITY] section. 3. Run a longer warm-up period before the analysis period of i
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  unit_conversions: []
coupling:
  chain_position: bidirectional
  native_formats:
  - unspecified
  exchange_format: unspecified
  coupling_role: earth_system
quality:
  has_format_spec: true
  has_skill_md: true
  has_triplets: true
  n_inputs: 1
  n_outputs: 6
  n_inputs_canonical: 1
  n_outputs_canonical: 5
  n_unmapped: 1
  n_traps: 28
  n_conversions: 0
  spatial_complete: true
  temporal_complete: true
  unmapped_variables:
  - name: water_age
    direction: output
    reason: unmapped
  validation_issues: []
  card_status: complete
