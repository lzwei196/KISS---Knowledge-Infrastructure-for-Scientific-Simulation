ata_card_version: '1.0'
generated: '2026-04-26'
generator: ata_pipeline1_card_gen.py
identity:
  model_id: DayCent
  model_name: DayCent (Daily Century Soil Organic Matter, Plant Production, and Trace-Gas
    Model)
  domain: biogeochemistry
  secondary_domains:
  - ghg_emissions
  - soil_carbon
  - crop_yield
  language: Fortran77
  binary_path: /mnt/disk1/Hydrocraft_server/model/daycent/bin/DD18centEVI_rev491
inputs:
- canonical_id: precipitation_rate
  local_name: precipitation
  unit: cm/day
  valid_range:
  - 0.0
  - 50.0
  format: ascii_text
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
  silent_error: precipitation in mm/day (the global default) instead of cm/day → soil
    saturated daily, runoff > 100 cm/yr. Convert with ÷10.
- canonical_id: air_temperature_max
  local_name: temperature_max
  unit: degC
  valid_range:
  - -50.0
  - 60.0
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
  silent_error: temperature in Kelvin (CMFD raw) → all rates wrong. Subtract 273.15.
- canonical_id: air_temperature_min
  local_name: temperature_min
  unit: degC
  valid_range:
  - -70.0
  - 50.0
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: shortwave_radiation
  local_name: solar_radiation
  unit: langleys/day
  valid_range:
  - 0.0
  - 800.0
  format: unspecified
  semantic_type: energy_flux
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: null
  local_name: soil_layers
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: static
- canonical_id: null
  local_name: schedule
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: static
- canonical_id: null
  local_name: cultivation_events
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: static
outputs:
- canonical_id: n2o_emission
  local_name: n2o_emission
  unit: gN/m2/day
  valid_range:
  - 0.0
  - 0.5
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: null
  local_name: nitrification_n2o
  unit: gN/m2/day
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
- canonical_id: null
  local_name: denitrification_n2o
  unit: gN/m2/day
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
- canonical_id: null
  local_name: no_emission
  unit: gN/m2/day
  valid_range:
  - 0.0
  - 0.2
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
- canonical_id: ch4_emission
  local_name: ch4_emission
  unit: gC/m2/day
  valid_range:
  - 0.0
  - 5.0
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: co2_flux
  local_name: co2_heterotrophic
  unit: gC/m2/day
  valid_range:
  - 0.0
  - 30.0
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: biomass
  local_name: aboveground_biomass
  unit: gC/m2
  valid_range:
  - 0.0
  - 5000.0
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: substring
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: biomass
  local_name: belowground_biomass
  unit: gC/m2
  valid_range: null
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: substring
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: grain_yield
  local_name: crop_yield
  unit: gC/m2
  valid_range: null
  format: unspecified
  semantic_type: rate
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: soil_organic_carbon
  local_name: soil_organic_carbon
  unit: gC/m2
  valid_range:
  - 0.0
  - 50000.0
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: soil_moisture
  local_name: soil_moisture_layers
  unit: m3/m3
  valid_range:
  - 0.0
  - 0.6
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: substring
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: soil_temperature
  local_name: soil_temperature_layers
  unit: degC
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: substring
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: null
  local_name: water_balance
  unit: cm/day
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  output_section: primary
  origin: unknown
  origin_confidence: low
- canonical_id: n2o_flux
  local_name: N2O
  unit: kgN/ha/yr
  semantic_type: flux_density
  mapping_confidence: inferred
  origin: simulated
  origin_confidence: high
  output_section: primary
  format: unspecified
  valid_range:
  - 0
  - null
spatial:
  type: network
  resolution: 40.78deg
  crs: EPSG:4326
  source: skill_md
temporal:
  resolution: daily
  calendar: standard
  source: format_spec
risks:
  known_traps:
  - canonical_var: precipitation_rate
    trap_id: '1'
    description: Soil moisture saturated every day; runoff in summary.out > 100 cm/yr.
    detection: Precipitation in the .wth file is in mm/day instead of cm/day.
    fix: Re-run convert_forcing_to_daycent.py — its validate_outputs() checks annual
      precip is 5–500 cm/yr. Manually verify column 7 of the .wth file is in cm/day,
      not mm/day.
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: '18'
    description: Annual precipitation in summary.out is exactly 1/10 of expected.
    detection: Converter divided cm/day inputs by 10 a second time. The auto-detect
      heuristic flagged a unit conversion that wasn't needed.
    fix: 'Bypass the heuristic: pre-divide your inputs to cm/day yourself, or use
      a forcing source whose units are unambiguous (NASA POWER returns mm/day reliably).'
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: '20'
    description: Crop yield (cgrain) is reasonable but cumulative SOC drops faster
      than expected.
    detection: OMAD (organic amendment) lines in the schedule reference an event name
      not present in omad.100, so the model adds zero amendment but does not warn.
    fix: grep the OMAD event name from the schedule against omad.100 — every OMAD
      line must reference an omad.100 entry by name.
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: '21'
    description: convert_forcing_to_daycent.py --source nasa_power fails with curl
      exit 35, SSL_ERROR_SYSCALL, or requests.exceptions.SSLError on power.larc.nasa.gov.
    detection: The host firewall or proxy blocks outbound TLS to power.larc.nasa.gov.
      NASA POWER is unreachable from this environment even though other hosts (google.com)
      respond. This is an environmental constraint
    fix: Do not retry. Switch to --source fluxnet for FLUXNET tower tasks (works offline
      from the site's FULLSET_DD.csv) or --source cmfd for Chinese sites. NASA POWER
      is not a viable fallback on this host.
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: '25'
    description: User asked for 'daily GPP' but few_outvars.txt lists agcprd/bgcprd/cgrain/somsc
      — none labeled GPP.
    detection: 'DayCent reports daily net C production as `cprodc` in summary.out
      (when few_outfiles.in is used). The .lis pathway via DDlist100 does not give
      a ready daily GPP variable; agcprd+bgcprd in the .lis is '
    fix: For daily GPP validation, parse summary.out's cprodc column directly instead
      of running DDlist100. The SKILL.md 'Mapping DayCent outputs to FLUXNET variables'
      table lists the canonical mappings.
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: air_temperature_max
    trap_id: '3'
    description: DayCent runs but Tmin > Tmax in stdout summary.
    detection: Columns 5 and 6 in the .wth file are swapped (Tmax in col 5, Tmin in
      col 6).
    fix: 'Check the load_daily_forcing dict keys: ''tmin'' must go to col 5, ''tmax''
      to col 6. The converter validates mean(Tmin) < mean(Tmax).'
    is_unit_trap: true
    automatable: false
    severity: high
  - trap_id: 1
    triplet_ref: ''
    description: Soil moisture saturated every day; runoff in summary.out > 100 cm/yr.
    affected_output: Precipitation in the .wth file is in mm/day instead of cm/day.
    detection: Precipitation in the .wth file is in mm/day instead of cm/day.
    fix: Re-run convert_forcing_to_daycent.py — its validate_outputs() checks annual
      precip is 5–500 cm/yr. Manually verify column 7 of the .wth file is in cm/day,
      not mm/day.
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: 2
    triplet_ref: ''
    description: All daily Tmin and Tmax appear ~270 in summary.out.
    affected_output: Temperature columns 5/6 of the .wth file are in Kelvin (CMFD
      raw).
    detection: Temperature columns 5/6 of the .wth file are in Kelvin (CMFD raw).
    fix: Subtract 273.15 from columns 5 and 6. The converter does this automatically
      when mean(T) > 150.
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: 3
    triplet_ref: ''
    description: DayCent runs but Tmin > Tmax in stdout summary.
    affected_output: Columns 5 and 6 in the .wth file are swapped (Tmax in col 5,
      Tmin in col 6).
    detection: Columns 5 and 6 in the .wth file are swapped (Tmax in col 5, Tmin in
      col 6).
    fix: 'Check the load_daily_forcing dict keys: ''tmin'' must go to col 5, ''tmax''
      to col 6. The converter validates mean(Tmin) < mean(Tmax).'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: 4
    triplet_ref: ''
    description: DayCent stops with 'unable to read line N of weather file'.
    affected_output: A .wth row has wrong number of columns or non-numeric content.
    detection: A .wth row has wrong number of columns or non-numeric content.
    fix: 'Run `awk ''{print NF}'' wooster.wth | sort -u` — every row should have 7
      fields. Check for stray header lines or comma decimal separators (locale issue:
      run with LC_ALL=C).'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 5
    triplet_ref: ''
    description: DayCent crashes during initialisation with 'cannot find <site>.100'.
    affected_output: The schedule file's third line names a site .100 file that is
      not in the run directory or library pa
    detection: The schedule file's third line names a site .100 file that is not in
      the run directory or library path.
    fix: Either copy <site>.100 into the run directory or run with `-l /path/to/100/lib`.
      Check the third line of the .sch file matches your filename exactly (case-sensitive).
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 6
    triplet_ref: ''
    description: soils.in column 3 (bulk density) values are 1230, 1300, 1340.
    affected_output: Bulk density was written in kg/m³ instead of g/cm³.
    detection: Bulk density was written in kg/m³ instead of g/cm³.
    fix: Divide by 1000. convert_soil_to_daycent.py auto-detects values > 100 and
      converts.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 7
    triplet_ref: ''
    description: DayCent runs but ANPP is 0 every year.
    affected_output: soils.in root-fraction column (col 7) sums to 0, so no roots
      can extract water.
    detection: soils.in root-fraction column (col 7) sums to 0, so no roots can extract
      water.
    fix: Check sum(col 7) ~ 1.0. The convert_soil_to_daycent.py validator warns when
      out of [0.95, 1.05].
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 8
    triplet_ref: ''
    description: Wilting point > field capacity in soils.in.
    affected_output: Column 4 (FC) and column 5 (WP) are swapped or ROSETTA returned
      absurd values for a sandy soil.
    detection: Column 4 (FC) and column 5 (WP) are swapped or ROSETTA returned absurd
      values for a sandy soil.
    fix: Validator raises 'FC <= WP' before the file is used. Re-check the ROSETTA
      inputs (sand+clay should be ≤ 1).
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 9
    triplet_ref: ''
    description: ACEQ run reaches the maximum block count without convergence.
    affected_output: Climate cycle in the .wth file is too short or has unrepresentative
      trend; SOM cannot stabilise.
    detection: Climate cycle in the .wth file is too short or has unrepresentative
      trend; SOM cannot stabilise.
    fix: Provide a 30+ year detrended climate cycle. Increase the +N value on the
      ACEQ line in the schedule. Re-check soils.in for unrealistic Ksat or root distribution.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 10
    triplet_ref: ''
    description: Equilibrium produces SOM C of 1500 g/m² in a temperate humid site.
    affected_output: Initial SOM pools in <site>.100 (som1ci, som2ci, som3ci) were
      not adjusted from a desert template.
    detection: Initial SOM pools in <site>.100 (som1ci, som2ci, som3ci) were not adjusted
      from a desert template.
    fix: Use 100_Files/site_template_rev491.100 as the base and edit only what you
      need. Check that strucc, metabc, som1c, som2c, som3c are non-zero and sum to
      a plausible total.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 11
    triplet_ref: ''
    description: Treatment run fails immediately with 'cannot read binary file base'.
    affected_output: 'Base history was not run before treatment, or `-e base` does
      not match the actual filename produced '
    detection: Base history was not run before treatment, or `-e base` does not match
      the actual filename produced by the previous step.
    fix: Check that base.bin exists in the run directory and is non-empty. Run `ls
      -la *.bin` between steps. The run_daycent.py wrapper enforces the correct order.
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: 12
    triplet_ref: ''
    description: Treatment harvest.csv is empty.
    affected_output: outfiles.in still has `harvest.csv` set to 0, OR there are no
      HARV events in the schedule.
    detection: outfiles.in still has `harvest.csv` set to 0, OR there are no HARV
      events in the schedule.
    fix: Set the harvest.csv toggle to 1 in outfiles.in before the treatment run.
      Verify the .sch file has HARV lines for the period you care about.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 13
    triplet_ref: ''
    description: Treatment run uses the wrong climate years.
    affected_output: The .sch file's `Weather choice` line points to a .wth file that
      does not span the treatment period;
    detection: The .sch file's `Weather choice` line points to a .wth file that does
      not span the treatment period; DayCent silently recycles the available years.
    fix: Make sure the .wth file covers Schedule Start..Schedule End. The wrapper
      run_daycent.py does not check this — verify manually.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 14
    triplet_ref: ''
    description: DDlist100 produces a .lis file with fewer columns than requested.
    affected_output: One or more variable names in the var-list file are misspelled;
      DDlist100 silently drops unknown nam
    detection: One or more variable names in the var-list file are misspelled; DDlist100
      silently drops unknown names.
    fix: 'Compare your varlist against the canonical names in DayCent_Manual_full_02.06.2024.pdf
      (search for ''List of Output Variables''). Common typos: aglivec instead of
      aglivc, somc instead of somsc.'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 15
    triplet_ref: ''
    description: parse_daycent_output.py says 'too few lines for .lis format'.
    affected_output: DDlist100 was given the wrong bin root (e.g. 'eq' instead of
      'cb_nt') and produced an almost-empty .
    detection: DDlist100 was given the wrong bin root (e.g. 'eq' instead of 'cb_nt')
      and produced an almost-empty .lis.
    fix: Check that the bin_root passed to DDlist100 corresponds to the .bin file
      you actually want to extract. Run `file *.bin` to see what you have.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 16
    triplet_ref: ''
    description: metrics.json shows pearson_r ~ 0 with very large RMSE.
    affected_output: 'Time matching mismatch: observed series uses calendar year but
      the simulated ''time'' column is decima'
    detection: 'Time matching mismatch: observed series uses calendar year but the
      simulated ''time'' column is decimal year, so nearest-year matching is picking
      the wrong rows.'
    fix: Check both 'time' columns in head -2 of the .lis and the obs CSV. parse_daycent_output.py
      uses ±1 year tolerance — narrow this if your obs are mid-year.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 17
    triplet_ref: ''
    description: DDlist100 segfaults or 'cannot open binary file'.
    affected_output: Trying to extract from a .bin produced by a different revision
      of DDcentEVI; binary format is not st
    detection: Trying to extract from a .bin produced by a different revision of DDcentEVI;
      binary format is not stable across revisions.
    fix: Use the matching DDlist100 version (rev 491 binary needs DDlist100_rev491).
      The two ship together in Linux_Version_491/.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 18
    triplet_ref: ''
    description: Annual precipitation in summary.out is exactly 1/10 of expected.
    affected_output: Converter divided cm/day inputs by 10 a second time. The auto-detect
      heuristic flagged a unit conver
    detection: Converter divided cm/day inputs by 10 a second time. The auto-detect
      heuristic flagged a unit conversion that wasn't needed.
    fix: 'Bypass the heuristic: pre-divide your inputs to cm/day yourself, or use
      a forcing source whose units are unambiguous (NASA POWER returns mm/day reliably).'
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: 19
    triplet_ref: ''
    description: DDcentEVI reports 'Negative pool detected, simulation aborted'.
    affected_output: 'A SOM or N pool went negative due to a parameter inconsistency
      — usually epnfa/epnfs (atmospheric N '
    detection: A SOM or N pool went negative due to a parameter inconsistency — usually
      epnfa/epnfs (atmospheric N inputs) too low, or fix.100 mismatched with site.100.
    fix: Verify fix.100 is the rev 491 version (in 100_Files/). Increase epnfa to
      0.05–0.10 g N/m²/yr if your site is N-limited.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 20
    triplet_ref: ''
    description: Crop yield (cgrain) is reasonable but cumulative SOC drops faster
      than expected.
    affected_output: OMAD (organic amendment) lines in the schedule reference an event
      name not present in omad.100, so t
    detection: OMAD (organic amendment) lines in the schedule reference an event name
      not present in omad.100, so the model adds zero amendment but does not warn.
    fix: grep the OMAD event name from the schedule against omad.100 — every OMAD
      line must reference an omad.100 entry by name.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 21
    triplet_ref: ''
    description: convert_forcing_to_daycent.py --source nasa_power fails with curl
      exit 35, SSL_ERROR_SYSCALL, or requests.exceptions.SSLError on power.larc.nasa.gov.
    affected_output: The host firewall or proxy blocks outbound TLS to power.larc.nasa.gov.
      NASA POWER is unreachable fro
    detection: The host firewall or proxy blocks outbound TLS to power.larc.nasa.gov.
      NASA POWER is unreachable from this environment even though other hosts (google.com)
      respond. This is an environmental constraint
    fix: Do not retry. Switch to --source fluxnet for FLUXNET tower tasks (works offline
      from the site's FULLSET_DD.csv) or --source cmfd for Chinese sites. NASA POWER
      is not a viable fallback on this host.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 22
    triplet_ref: ''
    description: convert_forcing_to_daycent.py --source mswx fails with FileNotFoundError
      inside /mnt/disk1/.../Data_forcing_03hr_010deg/P/P_YYYY.nc.
    affected_output: MSWX default path was mis-set to the CMFD root, and MSWX is not
      actually deployed on this host. --so
    detection: MSWX default path was mis-set to the CMFD root, and MSWX is not actually
      deployed on this host. --source mswx pointed at CMFD files and inherited the
      CMFD layout expectations.
    fix: If MSWX is truly needed, set --forcing-dir /path/to/mswx explicitly. For
      most tasks, use --source fluxnet (FLUXNET CSV) or --source cmfd (if the site
      is inside China).
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 23
    triplet_ref: ''
    description: convert_forcing_to_daycent.py --source cmfd fails or returns empty
      arrays for a non-China latitude/longitude (e.g. DE-Tha 50.96N 13.57E, Wooster
      40.78N 81.93W).
    affected_output: CMFD covers the China domain only (~70E-140E, ~15N-55N). Querying
      it outside this window either retu
    detection: CMFD covers the China domain only (~70E-140E, ~15N-55N). Querying it
      outside this window either returns no grid cell or raises FileNotFoundError
      on per-year NetCDFs. validate_inputs now hard-refuses C
    fix: Use --source fluxnet with the tower's FULLSET_DD.csv for FLUXNET sites, or
      --source nasa_power for global coverage (only if the host can reach power.larc.nasa.gov
      — see triplet 21).
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 24
    triplet_ref: ''
    description: Task asks for DayCent at a forest flux site (e.g. DE-Tha Norway spruce);
      tree.100 in WoosterExampleLinux is 0 bytes and no forest .sch template exists.
    affected_output: The public WoosterExampleLinux bundle ships an empty tree.100
      — the TREE submodel cannot run without
    detection: The public WoosterExampleLinux bundle ships an empty tree.100 — the
      TREE submodel cannot run without a populated parameter library, and CSU NREL
      distributes tree.100 separately.
    fix: Either obtain a populated tree.100 from CSU NREL and supply a forest .sch,
      or substitute a grassland (GRASS/GRAS1) parameterisation and explicitly flag
      this as a coarse surrogate in the run report. Do
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 25
    triplet_ref: ''
    description: User asked for 'daily GPP' but few_outvars.txt lists agcprd/bgcprd/cgrain/somsc
      — none labeled GPP.
    affected_output: DayCent reports daily net C production as `cprodc` in summary.out
      (when few_outfiles.in is used). Th
    detection: 'DayCent reports daily net C production as `cprodc` in summary.out
      (when few_outfiles.in is used). The .lis pathway via DDlist100 does not give
      a ready daily GPP variable; agcprd+bgcprd in the .lis is '
    fix: For daily GPP validation, parse summary.out's cprodc column directly instead
      of running DDlist100. The SKILL.md 'Mapping DayCent outputs to FLUXNET variables'
      table lists the canonical mappings.
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 26
    triplet_ref: ''
    description: DayCent crashes with 'swclimit[1] is negative. Check soils.in' during
      first timestep.
    affected_output: convert_soil_to_daycent.py wrote 12 columns but DayCent rev 491
      expects 13 columns. The missing colu
    detection: convert_soil_to_daycent.py wrote 12 columns but DayCent rev 491 expects
      13 columns. The missing column 11 (delta_min, minimum volumetric water content)
      causes DayCent to read Ksat into the delta_min s
    fix: 'Fixed in convert_soil_to_daycent.py (2026-04-22): added delta_min column
      (= WP × 0.9) between OM and Ksat. Verify soils.in has exactly 13 tab-separated
      columns per row. Cross-check against WoosterExam'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: 27
    triplet_ref: ''
    description: Soil organic matter in soils.in is 0.86 or 1.45 instead of 0.0086
      or 0.0145. DayCent runs without error but SOM decomposition is wildly overestimated.
    affected_output: HWSD returns OC as percent (e.g., 0.86% = 0.86). The old threshold
      (> 1.5) missed typical values 0.5
    detection: 'HWSD returns OC as percent (e.g., 0.86% = 0.86). The old threshold
      (> 1.5) missed typical values 0.5-1.5 which were written directly as OM fraction
      instead of being divided by 100. A value of 0.86 in '
    fix: 'Fixed in convert_soil_to_daycent.py (2026-04-22): lowered the OM threshold
      from > 1.5 to > 0.2. Any OM value > 0.2 is now treated as percent and divided
      by 100. Typical mineral soil OM fraction should'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  unit_conversions: []
coupling:
  chain_position: upstream_producer
  native_formats:
  - ascii_text
  exchange_format: ascii_text
  coupling_role: earth_system
quality:
  has_format_spec: true
  has_skill_md: true
  has_triplets: true
  n_inputs: 7
  n_outputs: 13
  n_inputs_canonical: 4
  n_outputs_canonical: 9
  n_unmapped: 7
  n_traps: 33
  n_conversions: 0
  spatial_complete: true
  temporal_complete: true
  unmapped_variables:
  - name: soil_layers
    direction: input
    reason: unmapped
  - name: schedule
    direction: input
    reason: unmapped
  - name: cultivation_events
    direction: input
    reason: unmapped
  - name: nitrification_n2o
    direction: output
    reason: unmapped
  - name: denitrification_n2o
    direction: output
    reason: unmapped
  - name: no_emission
    direction: output
    reason: unmapped
  - name: water_balance
    direction: output
    reason: unmapped
  validation_issues: []
  card_status: complete
