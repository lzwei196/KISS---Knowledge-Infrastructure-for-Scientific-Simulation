# ATA Card: DNDC
# Generated by Pipeline 1 on 2026-04-26

ata_card_version: '1.0'
generated: '2026-04-26'
generator: ata_pipeline1_card_gen.py
identity:
  model_id: DNDC
  model_name: DNDC (DNDCv.CAN)
  domain: biogeochemistry
  secondary_domains: []
  language: unknown
  binary_path: ''
inputs:
- canonical_id: precipitation_rate
  local_name: Precipitation
  unit: '**cm**'
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: null
  local_name: Radiation
  unit: Solar radiation
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: forcing
- canonical_id: wind_speed
  local_name: Wind
  unit: Wind speed
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
- canonical_id: null
  local_name: Humidity
  unit: Relative humidity
  valid_range: null
  format: unspecified
  semantic_type: unknown
  unit_confidence: documented
  mapping_confidence: unmapped
  input_category: forcing
- canonical_id: air_temperature
  local_name: Temperature
  unit: deg C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  input_category: forcing
outputs:
- canonical_id: soil_organic_carbon
  local_name: SOC
  unit: kgC/kg soil
  valid_range: null
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: air_temperature
  local_name: Temperature
  unit: deg C
  valid_range: null
  format: unspecified
  semantic_type: temperature
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: echoed_input
  origin_confidence: high
- canonical_id: evapotranspiration
  local_name: Evapotranspiration
  unit: cm
  valid_range: null
  format: unspecified
  semantic_type: flux_density
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: biomass
  local_name: Biomass
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: state
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
- canonical_id: grain_yield
  local_name: Yield
  unit: unknown
  valid_range: null
  format: unspecified
  semantic_type: rate
  unit_confidence: documented
  mapping_confidence: exact_alias
  output_section: primary
  origin: simulated
  origin_confidence: high
spatial:
  type: point
  resolution: unspecified
  crs: EPSG:4326
  source: skill_md
temporal:
  resolution: daily
  calendar: standard
  source: skill_md
risks:
  known_traps:
  - canonical_var: air_temperature
    trap_id: dt_003
    description: 'Model crashes immediately on first time step or produces temperatures above 200 degrees in log output.

      '
    detection: 'Temperature was supplied in Kelvin (e.g., 293 K) instead of degrees Celsius.  DNDC expects degrees Celsius
      for both Tmax and Tmin.

      '
    fix: 'Subtract 273.15 from all temperature values before writing the climate file.  Assert that Tmin < Tmax and both fall
      within [-60, 60] C.

      '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: grain_yield
    trap_id: dt_007
    description: 'Fertilizer effect on crop yield is far too strong or too weak.  Simulated N surplus is unrealistic.

      '
    detection: Fertilizer amount was entered as total product mass (e.g., kgUrea/ha) instead of kgN/ha for each individual
      N form.  DNDC expects the elemental nitrogen mass per hectare for each fertilizer applicatio
    fix: 'Convert fertilizer mass to elemental N: for urea multiply by 0.467 (46.7% N), for ammonium nitrate multiply by 0.340.  Enter
      each form separately in kgN/ha.

      '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: grain_yield
    trap_id: dt_013
    description: 'Crop biomass parameters produce implausible LAI or harvest index. The model may simulate yields 2x too high
      or too low relative to literature parameterizations.

      '
    detection: Crop biomass parameters (max biomass production, initial biomass) were entered in kgDM/ha (dry matter) instead
      of kgC/ha.  DNDC internally tracks carbon, so all biomass parameters must be in carbon un
    fix: 'Multiply dry-matter biomass values by 0.45 (approximate C fraction of plant tissue) to convert to kgC/ha.  Verify
      by comparing simulated peak biomass to literature ranges in kgC/ha.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: wind_speed
    trap_id: dt_015
    description: 'Model fails to find climate or soil input files.  Error message references a path with mixed separators
      or backslash characters.

      '
    detection: 'File paths in the .dnd configuration use Windows-style backslashes (C:\data\climate.txt) but the runtime environment
      expects forward slashes, or vice versa.  This is especially common when .dnd files '
    fix: Normalize all paths in the .dnd file to use forward slashes (/). On Linux/Wine, ensure paths are relative or use
      Unix-style absolute paths.  Use the path normalization utility in the convert_forcing t
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: wind_speed
    trap_id: dt_017
    description: 'DNDC fails to launch or immediately exits when run via Wine on Linux. Wine log shows path translation errors
      or "file not found" for DLLs.

      '
    detection: Wine drive mapping does not translate Unix paths correctly.  The DNDC executable or its .dnd files reference
      Windows drive letters (e.g., C:\) that are not properly mapped in the Wine prefix, or the Z
    fix: 'Verify Wine drive mappings with ''wine winecfg''.  Ensure Z:\ maps to /. Convert all .dnd paths to use the Wine
      drive prefix (e.g., Z:/home/user/data/).  Set WINEPREFIX explicitly before each run.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_001
    description: 'Simulated crop yield is near zero or the model reports permanent drought stress even though precipitation
      inputs look reasonable in the source data.

      '
    detection: 'Precipitation values were supplied in mm (the standard for most reanalysis and station data) but DNDC expects
      precipitation in cm.  A 50 mm rain event is read as 50 cm, which is physically impossible '
    fix: 'Divide all precipitation values by 10 (mm -> cm) before writing the DNDC climate input file.  Add an assertion:
      max daily precip should rarely exceed 30 cm.  Use the convert_forcing_to_dndc tool with '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_002
    description: Crop growth is drastically reduced or photosynthesis is near zero despite realistic temperature and precipitation.  Alternatively,
      the model produces unreasonably high biomass if radiation is too larg
    detection: Solar radiation was provided in W/m2 (instantaneous flux) instead of MJ/m2/day (daily integral).  DNDC expects
      MJ/m2/day.  Typical clear-sky W/m2 values (200-400) are an order of magnitude larger than
    fix: 'Convert W/m2 to MJ/m2/day by multiplying by 0.0864 (= 86400 s/day / 1e6 J/MJ).  Validate that resulting values fall
      within 0-35 MJ/m2/day.

      '
    is_unit_trap: true
    automatable: false
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_005
    description: 'Drainage is extremely slow or extremely fast compared to expectations; water table dynamics are unrealistic
      despite correct water table depth settings.

      '
    detection: 'Hydraulic conductivity was entered in cm/min (a common lab reporting unit) instead of m/hr as DNDC expects.  cm/min
      and m/hr differ by a factor of 0.6 (1 cm/min = 0.6 m/hr).

      '
    fix: 'Convert hydraulic conductivity from cm/min to m/hr by multiplying by 0.6. Typical Ksat for loam: 0.01-0.1 m/hr;
      for sand: 0.5-1.0 m/hr.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_008
    description: 'Reported yield is approximately half of expected values when compared to field observations or census data
      in dry-matter terms.

      '
    detection: DNDC reports grain yield in kgC/ha (carbon mass).  Users compare this directly to dry matter yield without
      applying the carbon-to-dry-matter conversion factor.  Dry matter is approximately 2x the carb
    fix: 'Multiply DNDC output yield (kgC/ha) by 2.22 (= 1/0.45) for a more precise dry-matter estimate, or by 2.0 as a common
      approximation. Document which conversion factor is used.

      '
    is_unit_trap: true
    automatable: true
    severity: high
  - canonical_var: precipitation_rate
    trap_id: dt_009
    description: 'Model crashes or produces a soil-profile error message during initialization.  Log may reference "layer
      depth" or "profile depth".

      '
    detection: 'The soil profile layers do not sum to exactly 2 m total depth.  DNDC requires the complete soil column to
      be 2 m (200 cm) and will reject or crash if layers sum to a different depth.

      '
    fix: 'Ensure all soil layer thicknesses sum to exactly 200 cm (2.0 m). Adjust the deepest layer thickness as a residual
      to enforce the 2 m constraint.  Validate with: sum(layer_depths) == 200.

      '
    is_unit_trap: false
    automatable: false
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: dt_010
    description: 'Water table simulation is the opposite of what was intended: the model simulates a high water table when
      the user intended free drainage, or vice versa.

      '
    detection: 'The water table flag in DNDC uses reversed logic: 0 = water table IS present (Yes), 1 = water table is NOT
      present (No).  Users commonly assume 1 = Yes.

      '
    fix: 'Set water_table_flag = 0 to enable water table simulation; set water_table_flag = 1 to disable it (free drainage).  Always
      verify by inspecting the .dnd XML for the correct Boolean interpretation.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
  - canonical_var: precipitation_rate
    trap_id: dt_019
    description: 'Model runs to completion but simulation time is 10-100x longer than expected.  CPU usage is at 100% for
      extended periods.

      '
    detection: The dynamic time-stepping in the soil water solver is thrashing due to conflicting parameter settings (e.g.,
      very high Ksat with very thin layers, or extreme precipitation events with small convergenc
    fix: 'Review Ksat, layer depth, and convergence tolerance for consistency. Increase minimum dynamic layer depth.  Consider
      capping extreme precipitation events at physically reasonable daily maxima.

      '
    is_unit_trap: false
    automatable: false
    severity: medium
  - trap_id: dt_001
    triplet_ref: ''
    description: 'Simulated crop yield is near zero or the model reports permanent drought stress even though precipitation
      inputs look reasonable in the source data.

      '
    affected_output: Precipitation values were supplied in mm (the standard for most reanalysis and station data) but DND
    detection: 'Precipitation values were supplied in mm (the standard for most reanalysis and station data) but DNDC expects
      precipitation in cm.  A 50 mm rain event is read as 50 cm, which is physically impossible '
    fix: 'Divide all precipitation values by 10 (mm -> cm) before writing the DNDC climate input file.  Add an assertion:
      max daily precip should rarely exceed 30 cm.  Use the convert_forcing_to_dndc tool with '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_002
    triplet_ref: ''
    description: Crop growth is drastically reduced or photosynthesis is near zero despite realistic temperature and precipitation.  Alternatively,
      the model produces unreasonably high biomass if radiation is too larg
    affected_output: Solar radiation was provided in W/m2 (instantaneous flux) instead of MJ/m2/day (daily integral).  DN
    detection: Solar radiation was provided in W/m2 (instantaneous flux) instead of MJ/m2/day (daily integral).  DNDC expects
      MJ/m2/day.  Typical clear-sky W/m2 values (200-400) are an order of magnitude larger than
    fix: 'Convert W/m2 to MJ/m2/day by multiplying by 0.0864 (= 86400 s/day / 1e6 J/MJ).  Validate that resulting values fall
      within 0-35 MJ/m2/day.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_003
    triplet_ref: ''
    description: 'Model crashes immediately on first time step or produces temperatures above 200 degrees in log output.

      '
    affected_output: Temperature was supplied in Kelvin (e.g., 293 K) instead of degrees Celsius.  DNDC expects degrees C
    detection: 'Temperature was supplied in Kelvin (e.g., 293 K) instead of degrees Celsius.  DNDC expects degrees Celsius
      for both Tmax and Tmin.

      '
    fix: 'Subtract 273.15 from all temperature values before writing the climate file.  Assert that Tmin < Tmax and both fall
      within [-60, 60] C.

      '
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_004
    triplet_ref: ''
    description: 'Soil organic carbon decomposition rates are unrealistically high or the model reports SOC values orders
      of magnitude above literature ranges. Simulated N2O emissions may be 100x expected.

      '
    affected_output: SOC was entered as a percentage (e.g., 2.5 meaning 2.5%) instead of kgC/kg (e.g., 0.025).  DNDC expe
    detection: 'SOC was entered as a percentage (e.g., 2.5 meaning 2.5%) instead of kgC/kg (e.g., 0.025).  DNDC expects SOC
      as a mass fraction (kgC/kg).

      '
    fix: 'Divide SOC percentage values by 100 to convert to kgC/kg.  Validate that SOC values fall within 0.001-0.30 kgC/kg
      for mineral soils.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_005
    triplet_ref: ''
    description: 'Drainage is extremely slow or extremely fast compared to expectations; water table dynamics are unrealistic
      despite correct water table depth settings.

      '
    affected_output: Hydraulic conductivity was entered in cm/min (a common lab reporting unit) instead of m/hr as DNDC e
    detection: 'Hydraulic conductivity was entered in cm/min (a common lab reporting unit) instead of m/hr as DNDC expects.  cm/min
      and m/hr differ by a factor of 0.6 (1 cm/min = 0.6 m/hr).

      '
    fix: 'Convert hydraulic conductivity from cm/min to m/hr by multiplying by 0.6. Typical Ksat for loam: 0.01-0.1 m/hr;
      for sand: 0.5-1.0 m/hr.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_006
    triplet_ref: ''
    description: 'Field capacity and wilting point behave unexpectedly; the model simulates either waterlogged or bone-dry
      conditions that contradict the input values.

      '
    affected_output: Field capacity and wilting point were entered as volumetric water content (e.g., 0.33 m3/m3) but DND
    detection: Field capacity and wilting point were entered as volumetric water content (e.g., 0.33 m3/m3) but DNDC expects
      water-filled pore space (WFPS) fractions (e.g., FC at WFPS = 0.66 for porosity 0.50).  WFP
    fix: 'Divide volumetric water content by soil porosity to obtain WFPS. FC_wfps = FC_vol / porosity.  WP_wfps = WP_vol
      / porosity.  Validate that 0 < WP_wfps < FC_wfps < 1.0.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_007
    triplet_ref: ''
    description: 'Fertilizer effect on crop yield is far too strong or too weak.  Simulated N surplus is unrealistic.

      '
    affected_output: Fertilizer amount was entered as total product mass (e.g., kgUrea/ha) instead of kgN/ha for each ind
    detection: Fertilizer amount was entered as total product mass (e.g., kgUrea/ha) instead of kgN/ha for each individual
      N form.  DNDC expects the elemental nitrogen mass per hectare for each fertilizer applicatio
    fix: 'Convert fertilizer mass to elemental N: for urea multiply by 0.467 (46.7% N), for ammonium nitrate multiply by 0.340.  Enter
      each form separately in kgN/ha.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_008
    triplet_ref: ''
    description: 'Reported yield is approximately half of expected values when compared to field observations or census data
      in dry-matter terms.

      '
    affected_output: DNDC reports grain yield in kgC/ha (carbon mass).  Users compare this directly to dry matter yield w
    detection: DNDC reports grain yield in kgC/ha (carbon mass).  Users compare this directly to dry matter yield without
      applying the carbon-to-dry-matter conversion factor.  Dry matter is approximately 2x the carb
    fix: 'Multiply DNDC output yield (kgC/ha) by 2.22 (= 1/0.45) for a more precise dry-matter estimate, or by 2.0 as a common
      approximation. Document which conversion factor is used.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_009
    triplet_ref: ''
    description: 'Model crashes or produces a soil-profile error message during initialization.  Log may reference "layer
      depth" or "profile depth".

      '
    affected_output: The soil profile layers do not sum to exactly 2 m total depth.  DNDC requires the complete soil colu
    detection: 'The soil profile layers do not sum to exactly 2 m total depth.  DNDC requires the complete soil column to
      be 2 m (200 cm) and will reject or crash if layers sum to a different depth.

      '
    fix: 'Ensure all soil layer thicknesses sum to exactly 200 cm (2.0 m). Adjust the deepest layer thickness as a residual
      to enforce the 2 m constraint.  Validate with: sum(layer_depths) == 200.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_010
    triplet_ref: ''
    description: 'Water table simulation is the opposite of what was intended: the model simulates a high water table when
      the user intended free drainage, or vice versa.

      '
    affected_output: 'The water table flag in DNDC uses reversed logic: 0 = water table IS present (Yes), 1 = water table '
    detection: 'The water table flag in DNDC uses reversed logic: 0 = water table IS present (Yes), 1 = water table is NOT
      present (No).  Users commonly assume 1 = Yes.

      '
    fix: 'Set water_table_flag = 0 to enable water table simulation; set water_table_flag = 1 to disable it (free drainage).  Always
      verify by inspecting the .dnd XML for the correct Boolean interpretation.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_011
    triplet_ref: ''
    description: 'Model crashes or reads incorrect climate data for the first days of the simulation.  Time series appears
      shifted.

      '
    affected_output: The climate input file does not start at Julian day 1 (January 1).  DNDC expects the first record to
    detection: The climate input file does not start at Julian day 1 (January 1).  DNDC expects the first record to correspond
      to Jday 1 and reads sequentially. A file starting at Jday 0 or at an arbitrary date will
    fix: 'Ensure the climate file begins with Jday 1 data.  If the source data starts mid-year, pad with synthetic or climatological
      values for days 1 through start_day-1.  Validate: first_record_jday == 1.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_012
    triplet_ref: ''
    description: 'Model produces a corrupted or unreadable .dnd file.  GUI may crash when loading the file.  Certain simulation
      years contain garbage values.

      '
    affected_output: The .dnd file time block structure was not built linearly.  DNDC expects time blocks (year-by-year m
    detection: The .dnd file time block structure was not built linearly.  DNDC expects time blocks (year-by-year management
      sequences) to be written in strict chronological order.  Programmatic generation that inse
    fix: Build .dnd time blocks in strictly ascending chronological order (year 1, year 2, ..., year N).  Never insert, delete,
      or reorder year blocks after initial creation.  Validate with a sequential year-c
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_013
    triplet_ref: ''
    description: 'Crop biomass parameters produce implausible LAI or harvest index. The model may simulate yields 2x too high
      or too low relative to literature parameterizations.

      '
    affected_output: Crop biomass parameters (max biomass production, initial biomass) were entered in kgDM/ha (dry matte
    detection: Crop biomass parameters (max biomass production, initial biomass) were entered in kgDM/ha (dry matter) instead
      of kgC/ha.  DNDC internally tracks carbon, so all biomass parameters must be in carbon un
    fix: 'Multiply dry-matter biomass values by 0.45 (approximate C fraction of plant tissue) to convert to kgC/ha.  Verify
      by comparing simulated peak biomass to literature ranges in kgC/ha.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_014
    triplet_ref: ''
    description: 'After editing a heterogeneous soil profile, SOC pool fractions (labile, resistant, humads, humus) silently
      reset to defaults.  Subsequent simulation starts with wrong initial C pools.

      '
    affected_output: The DNDC GUI resets SOC pool partitioning fractions to default values when any soil property in a he
    detection: 'The DNDC GUI resets SOC pool partitioning fractions to default values when any soil property in a heterogeneous
      (multi-layer) profile is edited.  The reset is silent and not logged.

      '
    fix: 'After any soil edit in a heterogeneous profile, manually re-enter or verify SOC pool fractions.  Better: script
      the .dnd file generation to avoid GUI-induced resets.  Always diff the .dnd before and a'
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_015
    triplet_ref: ''
    description: 'Model fails to find climate or soil input files.  Error message references a path with mixed separators
      or backslash characters.

      '
    affected_output: File paths in the .dnd configuration use Windows-style backslashes (C:\data\climate.txt) but the run
    detection: 'File paths in the .dnd configuration use Windows-style backslashes (C:\data\climate.txt) but the runtime environment
      expects forward slashes, or vice versa.  This is especially common when .dnd files '
    fix: Normalize all paths in the .dnd file to use forward slashes (/). On Linux/Wine, ensure paths are relative or use
      Unix-style absolute paths.  Use the path normalization utility in the convert_forcing t
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_016
    triplet_ref: ''
    description: 'Batch run fails even though individual .dnd files run correctly in the GUI.  Error log shows "file not found"
      for .dnd paths.

      '
    affected_output: The batch control file (.bat or batch list) contains paths that do not match the internal paths stor
    detection: The batch control file (.bat or batch list) contains paths that do not match the internal paths stored in each
      .dnd file.  DNDC resolves some paths relative to the .dnd file location and others relati
    fix: 'Ensure batch file paths are absolute and consistent with the paths embedded inside each .dnd file.  Run a validation
      script that parses each .dnd and confirms all referenced files exist at the stated '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_017
    triplet_ref: ''
    description: 'DNDC fails to launch or immediately exits when run via Wine on Linux. Wine log shows path translation errors
      or "file not found" for DLLs.

      '
    affected_output: Wine drive mapping does not translate Unix paths correctly.  The DNDC executable or its .dnd files r
    detection: Wine drive mapping does not translate Unix paths correctly.  The DNDC executable or its .dnd files reference
      Windows drive letters (e.g., C:\) that are not properly mapped in the Wine prefix, or the Z
    fix: 'Verify Wine drive mappings with ''wine winecfg''.  Ensure Z:\ maps to /. Convert all .dnd paths to use the Wine
      drive prefix (e.g., Z:/home/user/data/).  Set WINEPREFIX explicitly before each run.

      '
    is_unit_trap: true
    automatable: true
    severity: high
    source: format_spec
  - trap_id: dt_018
    triplet_ref: ''
    description: 'Model crashes with a segmentation fault or access violation during soil water dynamics computation.  Crash
      occurs in the first few time steps.

      '
    affected_output: The dynamic (adaptive) layer depth in the soil thermal/water solver is set too small (e.g., < 1 cm),
    detection: The dynamic (adaptive) layer depth in the soil thermal/water solver is set too small (e.g., < 1 cm), causing
      the numerical solver to create an excessive number of sub-layers that overflow internal arr
    fix: Set the minimum dynamic layer depth to at least 2 cm.  If high resolution is needed, increase gradually and test
      stability.  Check the .dnd file for the layer_min_depth parameter and ensure it is >= 0
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_019
    triplet_ref: ''
    description: 'Model runs to completion but simulation time is 10-100x longer than expected.  CPU usage is at 100% for
      extended periods.

      '
    affected_output: The dynamic time-stepping in the soil water solver is thrashing due to conflicting parameter setting
    detection: The dynamic time-stepping in the soil water solver is thrashing due to conflicting parameter settings (e.g.,
      very high Ksat with very thin layers, or extreme precipitation events with small convergenc
    fix: 'Review Ksat, layer depth, and convergence tolerance for consistency. Increase minimum dynamic layer depth.  Consider
      capping extreme precipitation events at physically reasonable daily maxima.

      '
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_020
    triplet_ref: ''
    description: 'Simulated annual N2O emissions are zero or near-zero despite significant N fertilizer inputs and expected
      denitrification conditions.

      '
    affected_output: 'DNDC may silently skip denitrification calculations if soil pH is outside the valid range or if the '
    detection: DNDC may silently skip denitrification calculations if soil pH is outside the valid range or if the anaerobic
      balloon fraction is suppressed by incorrect WFPS parameterization.  No error is raised; th
    fix: Verify soil pH is within 3.5-9.0.  Check that WFPS parameterization allows anaerobic microsites to form (FC_wfps
      should be > 0.5 for typical soils).  Compare simulated WFPS time series against expecte
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_021
    triplet_ref: ''
    description: 'Output files contain plausible-looking numbers but cover fewer years than specified in the simulation period.  No
      error or warning is produced.

      '
    affected_output: The climate file contains fewer days than the simulation period requests.  DNDC silently truncates t
    detection: 'The climate file contains fewer days than the simulation period requests.  DNDC silently truncates the simulation
      when climate data runs out rather than raising an error.

      '
    fix: Validate that the climate file contains at least (n_years * 365) records (account for leap years if applicable).  Add
      a pre-run check that compares climate file line count to expected simulation lengt
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  - trap_id: dt_022
    triplet_ref: ''
    description: 'Tillage or harvest events appear to have no effect on simulated SOC or crop residue dynamics.  Management
      schedule seems to be ignored.

      '
    affected_output: Management event dates fall outside the crop growing season or on Julian days that do not exist in t
    detection: 'Management event dates fall outside the crop growing season or on Julian days that do not exist in the climate
      record.  DNDC silently skips events scheduled for dates beyond the available time series '
    fix: Ensure all management event dates (planting, fertilization, tillage, harvest) fall within Jday 1-365 and within the
      climate record period. Cross-check event dates against the crop calendar for the tar
    is_unit_trap: false
    automatable: true
    severity: medium
    source: format_spec
  unit_conversions: []
coupling:
  chain_position: bidirectional
  native_formats:
  - unspecified
  exchange_format: unspecified
  coupling_role: earth_system
quality:
  has_format_spec: true
  has_skill_md: true
  has_triplets: true
  n_inputs: 5
  n_outputs: 5
  n_inputs_canonical: 3
  n_outputs_canonical: 5
  n_unmapped: 2
  n_traps: 34
  n_conversions: 0
  spatial_complete: true
  temporal_complete: true
  unmapped_variables:
  - name: Radiation
    direction: input
    reason: unmapped
  - name: Humidity
    direction: input
    reason: unmapped
  validation_issues: []
  card_status: complete
