# Coupling Config v2: Pywr -> CaMa_Flood

coupling_config_version: '2.0'
generated: '2026-05-16'
generator: ata_pipeline2_coupling_gen.py v2
metadata:
  edge_id: Pywr_to_CaMa_Flood
  source_model: Pywr
  target_model: CaMa_Flood
  source_domain: water_resources
  target_domain: routing
  edge_type: type5_cross_scale_cross_domain
  n_forward_variables: 1
  n_combine_variables: 0
  n_plausible_feedback: 0
  n_review_feedback: 0
  is_feedback: false
forward_edges:
- canonical_id: surface_runoff
  source_canonical_id: discharge
  target_canonical_id: surface_runoff
  match_type: family_match
  source_local_name: release_discharge
  source_unit: m3/s
  target_local_name: runoff
  target_unit: mm/day
combine_edges: []
plausible_feedback: []
review_feedback: []
compatibility:
- canonical_id: surface_runoff
  source_canonical_id: discharge
  target_canonical_id: surface_runoff
  match_type: family_match
  primitives_needed:
  - transform_semantic
  - exchange_data
  - map_space
  - validate_boundary
  details:
    transform_semantic: &id001
      type: volumetric_to_flux_density
      transform: runoff_mm_day = discharge_m3s * 86400 / cell_area_m2 * 1000
      requires:
      - cell_area_m2
      source_canonical: discharge
      target_canonical: surface_runoff
      note: Physical quantity changes — this is NOT a unit conversion
      canonical_id: surface_runoff
    convert_units: &id002
      status: handled_by_semantic_transform
      canonical_id: surface_runoff
    exchange_data: &id003
      from_format: csv
      to_format: netcdf
      conversion_needed: true
      variable_mapping:
        source_name: release_discharge
        target_name: runoff
      canonical_id: surface_runoff
    map_space: &id004
      from_type: network
      to_type: regular_grid
      method: unknown
      canonical_id: surface_runoff
    family_mapping: &id005
      source_canonical: discharge
      target_canonical: surface_runoff
      note: Source provides discharge, target needs surface_runoff. These are in the same family (surface_water_flux) — may
        need splitting/derivation.
      canonical_id: surface_runoff
    validate_boundary: &id006
      checks:
      - no_nan
      - 'range: [0.0, 500.0]'
      canonical_id: surface_runoff
primitives_needed:
- exchange_data
- map_space
- transform_semantic
- validate_boundary
execution_order:
- transform_semantic
- map_space
- exchange_data
- validate_boundary
primitive_configs:
  transform_semantic:
  - *id001
  convert_units:
  - *id002
  exchange_data:
  - *id003
  map_space:
  - *id004
  family_mapping:
  - *id005
  validate_boundary:
  - *id006
risks:
  total_traps: 20
  automatable: 17
  judgment_required: 3
  traps:
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: inpmat_grid_mismatch
    description: Inpmat WESTIN/EASTIN uses CaMa domain extent instead of VIC grid extent
    detection: outflw ~ 0 despite non-zero input runoff
    fix: Regenerate inpmat using VIC grid extent from basin_grid.nc
    is_unit_trap: false
    automatable: true
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: lat_order_ascending
    description: Runoff NetCDF latitude in ascending order instead of descending
    detection: ''
    fix: Ensure latitude is north-to-south (descending) in runoff NetCDF
    is_unit_trap: false
    automatable: false
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: unit_mismatch_m3s
    description: Runoff provided in m3/s instead of mm/day
    detection: ''
    fix: Keep runoff in mm/day — CaMa converts internally using ctmare.bin
    is_unit_trap: true
    automatable: false
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: conv_runoff_input
    description: 'Conversion trap: VIC (mm/day, OUT_RUNOFF + OUT_BASEFLOW) -> CaMa-Flood NetCDF (mm/day)'
    detection: If m3/s provided, discharge is ~1e6 too high for large basins
    fix: Apply none (same unit) factor= offset=
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: dt_cama_002
    description: '{''description'': ''CaMa-Flood runs without error but outflw values are near-zero everywhere despite VIC
      producing reasonable runoff'', ''error_pattern'': '''', ''detection_method'': ''value_comparison''}'
    detection: '{''root_cause'': ''The inpmat generation script uses the CaMa domain extent instead of the VIC grid extent
      for WESTIN/EASTIN/NORTHIN/SOUTHIN parameters'', ''explanation'': ''The generate_inpmat tool creates '
    fix: '{''action'': ''Regenerate inpmat using the VIC grid extent from basin_grid.nc, not the CaMa domain extent'', ''tool_to_run'':
      '''', ''manual_steps'': [''Read VIC grid extent from basin_grid.nc: python -c "import'
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: dt_cama_006
    description: '{''description'': ''CaMa receives runoff but many cells show zero flux -- NetCDF grid is padded with zeros
      around actual VIC data'', ''error_pattern'': '''', ''detection_method'': ''value_comparison''}'
    detection: '{''root_cause'': ''Runoff conversion script created a padded rectangular grid larger than the actual VIC domain'',
      ''explanation'': ''When converting VIC output to CaMa-Flood NetCDF, the script must create a'
    fix: '{''action'': ''Regenerate NetCDF using only the actual VIC grid extent'', ''tool_to_run'': ''tools/prepare_runoff_input.py'',
      ''manual_steps'': [''Check NC grid: python -c "import xarray as xr; print(xr.open_dat'
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: dt_cama_007
    description: '{''description'': ''CaMa-Flood runs to completion but outflw values are all near-zero despite non-zero runoff
      in input NetCDF'', ''error_pattern'': '''', ''detection_method'': ''value_comparison''}'
    detection: '{''root_cause'': ''The inpmat latitude ordering (OLAT) does not match the runoff NetCDF latitude ordering'',
      ''explanation'': "The inpmat maps each CaMa cell to VIC runoff grid cells. This depends on the la'
    fix: '{''action'': ''Verify lat ordering in runoff NC matches OLAT, regenerate inpmat if mismatched'', ''tool_to_run'':
      '''', ''manual_steps'': [''Check NC lat: python -c "import xarray as xr; ds=xr.open_dataset(\''pat'
    is_unit_trap: false
    automatable: true
  - canonical_var: surface_runoff
    model: CaMa_Flood
    trap_id: dt_cama_009
    description: '{''description'': ''CaMa-Flood produces discharge values that are orders of magnitude too high (e.g., millions
      of m3/s for a small basin)'', ''error_pattern'': '''', ''detection_method'': ''value_comparison''}'
    detection: '{''root_cause'': ''Runoff input was pre-converted to m3/s before feeding to CaMa-Flood, but CaMa expects mm/day
      and converts internally'', ''explanation'': ''CaMa-Flood reads gridded runoff in mm/day and con'
    fix: '{''action'': "Verify runoff input units are mm/day. Check the Runoff variable''s ''units'' attribute.", ''tool_to_run'':
      ''tools/prepare_runoff_input.py'', ''manual_steps'': [''python -c "import xarray as xr; ds='
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: Pywr
    trap_id: cama_injection_negative
    description: CaMa injection produces negative runoff — cell area unit error
    detection: Negative runoff values in CaMa input
    fix: Check cell area units in m3/s -> mm/day conversion
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: Pywr
    trap_id: dt_pywr_011
    description: '{''description'': ''After injecting Pywr releases into CaMa-Flood runoff NetCDF, some cells have negative
      runoff values. CaMa-Flood may produce NaN discharge or crash.'', ''error_pattern'': '''', ''detection_m'
    detection: '{''root_cause'': ''The injection replaces VIC runoff with Pywr release, but the unit conversion (m3/s to mm/day)
      is wrong, or the cell area used for conversion is incorrect.'', ''explanation'': ''inject_rele'
    fix: '{''action'': ''Verify cell area and unit conversion.'', ''tool_to_run'': ''inject_releases_to_cama'', ''manual_steps'':
      [''Check cell_area in output JSON: should be ~600-800 km2 for 0.25 deg at mid-latitudes'', '''
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: Pywr
    trap_id: dt_pywr_015
    description: '{''description'': ''Inflow values are unrealistically small (< 0.001 m3/s for a large basin) or unrealistically
      large (> 100,000 m3/s for a small basin). Model runs but results are wrong.'', ''error_patter'
    detection: '{''root_cause'': ''Unit conversion error when converting VIC runoff (mm/day) to inflow (m3/s). The correct
      formula is: Q_m3s = (runoff_mm + baseflow_mm) * cell_area_m2 / (1000 * 86400). Missing the /1000'
    fix: '{''action'': ''Verify unit conversion formula and cell area.'', ''tool_to_run'': ''convert_vic_to_inflow'', ''manual_steps'':
      [''Check formula: Q = (R+B) * A / (1000 * 86400)'', ''Check cell area: at 0.25 deg, mid'
    is_unit_trap: true
    automatable: true
  - canonical_var: discharge
    model: CaMa_Flood
    trap_id: dt_cama_003
    description: '{''description'': ''CaMa-Flood runs but river channel widths are unrealistically wide or narrow, causing
      wrong water depths and discharge timing'', ''error_pattern'': '''', ''detection_method'': ''value_comparis'
    detection: '{''root_cause'': ''calc_rivwth was run using the global diminfo file instead of the regional diminfo file'',
      ''explanation'': ''The calc_rivwth tool reads the diminfo file to determine grid dimensions (nrows'
    fix: '{''action'': ''Re-run calc_rivwth with the regional diminfo file'', ''tool_to_run'': '''', ''manual_steps'': [''cd
      map/{basin}_15min'', ''Verify regional diminfo exists: cat diminfo_{basin}_025deg.txt'', ''Run: ./sr'
    is_unit_trap: true
    automatable: true
  - canonical_var: discharge
    model: CaMa_Flood
    trap_id: dt_cama_005
    description: '{''description'': ''CaMa-Flood produces discharge at completely wrong locations -- river network does not
      match the basin'', ''error_pattern'': '''', ''detection_method'': ''value_comparison''}'
    detection: '{''root_cause'': ''CaMa map files from a previous basin were reused instead of regenerating for the new basin'',
      ''explanation'': "Each basin requires its own set of CaMa-Flood map files because the river n'
    fix: '{''action'': ''Regenerate all CaMa map files for the new basin'', ''tool_to_run'': ''tools/configure_simulation.py'',
      ''manual_steps'': [''Create new map dir: mkdir map/{new_basin}_15min'', ''Run configure_simulat'
    is_unit_trap: true
    automatable: false
  - canonical_var: discharge
    model: Pywr
    trap_id: inflow_all_zeros
    description: VIC inflow conversion produces all zeros
    detection: Inflow CSV values all 0.0
    fix: Check VIC flux file format, upstream cell selection, unit conversion
    is_unit_trap: false
    automatable: true
  - canonical_var: discharge
    model: Pywr
    trap_id: vic_conversion_error
    description: VIC mm/day to m3/s missing /1000 or /86400
    detection: Inflow magnitude unrealistic for basin size
    fix: Q (m3/s) = runoff (mm/day) * area (m2) / (1000 * 86400)
    is_unit_trap: true
    automatable: true
  - canonical_var: discharge
    model: Pywr
    trap_id: dt_pywr_005
    description: '{''description'': ''Pywr model runs but reservoir never fills. Storage stays at initial or decreases monotonically.
      Inflow CSV has all zeros or near-zero values.'', ''error_pattern'': '''', ''detection_method'''
    detection: '{''root_cause'': ''Wrong upstream cells selected in convert_vic_to_inflow.py. Either (a) VIC flux files not
      found for the coordinates, (b) wrong coordinate format in filenames, or (c) all cells are downs'
    fix: '{''action'': ''Check VIC flux filename format and upstream cell count.'', ''tool_to_run'': ''convert_vic_to_inflow'',
      ''manual_steps'': [''ls VIC result directory to see actual filename format'', ''Check cells_fou'
    is_unit_trap: true
    automatable: true
  - canonical_var: discharge
    model: Pywr
    trap_id: dt_pywr_008
    description: '{''description'': ''Release timeseries contains negative values. This is physically impossible (water cannot
      flow upstream into a reservoir through the release works).'', ''error_pattern'': '''', ''detection_m'
    detection: '{''root_cause'': ''Sign convention error in Pywr results extraction. Pywr itself cannot produce negative flows,
      but the recording or extraction code may negate values incorrectly.'', ''explanation'': "Pywr'''
    fix: '{''action'': ''Verify recorder setup and extraction logic.'', ''tool_to_run'': ''run_pywr'', ''manual_steps'': ["Check
      that recorders are type ''numpyarraynode'' (not ''numpyarraylink'')", ''Verify flow = recorder.d'
    is_unit_trap: false
    automatable: true
  - canonical_var: discharge
    model: Pywr
    trap_id: dt_pywr_013
    description: '{''description'': ''Pywr model runs but produces truncated results or errors about missing data at certain
      dates. The inflow CSV does not cover the full simulation period.'', ''error_pattern'': ''index.*out '
    detection: '{''root_cause'': ''The timestepper start/end dates in the model JSON do not match the date range in the inflow
      CSV. Pywr requests data for dates that are outside the CSV range.'', ''explanation'': "Pywr''s t'
    fix: '{''action'': ''Align model dates with inflow CSV date range.'', ''tool_to_run'': ''assemble_pywr_model'', ''manual_steps'':
      [''Check inflow CSV: head -1 and tail -1 to see date range'', ''Set --start_date to match'
    is_unit_trap: false
    automatable: true
  - canonical_var: discharge
    model: Pywr
    trap_id: dt_pywr_014
    description: '{''description'': ''Reservoir fills instantly and spills every day, or never fills despite large inflow.
      Storage volume scale is wrong by factor of 1e6.'', ''error_pattern'': '''', ''detection_method'': ''value_'
    detection: '{''root_cause'': ''GRanD CAP_MCM is in million cubic meters (MCM). If used directly as max_volume in Pywr
      (which expects m3), the capacity is 1e6 too small. Example: Meishan 2275 MCM -> should be 2.275e9'
    fix: '{''action'': ''Ensure max_volume = CAP_MCM * 1e6.'', ''tool_to_run'': ''build_reservoir_properties'', ''manual_steps'':
      [''Verify: max_volume_m3 = capacity_mcm * 1e6'', ''Sanity check: max_volume_m3 for a major da'
    is_unit_trap: true
    automatable: true
  - canonical_var: surface_runoff
    model: Pywr->CaMa_Flood
    trap_id: unit_mismatch_surface_runoff
    description: 'Unit mismatch: source [m3/s] vs target [mm/day]'
    detection: Check surface_runoff units at coupling boundary
    fix: Convert m3/s -> mm/day
    is_unit_trap: true
    automatable: true
